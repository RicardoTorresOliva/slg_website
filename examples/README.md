# examples/

Reference examples only. Content here is **never constitutive**: the core (`AGENTS.md`, `commands/`,
`profiles/`) must never depend on or reference anything in this folder. Example knowledge bundles
must be clearly labeled as examples (see `profiles/_contract.md`, rule 4).

This folder ships empty in the template.
