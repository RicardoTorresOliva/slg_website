# CLAUDE.md — pointer (Claude Code adapter)

This project is governed by **`AGENTS.md`** (the single, platform-neutral source of governance).

Read and follow `AGENTS.md`. Do not treat this file as a separate rule set — it only exists because
Claude Code auto-loads `CLAUDE.md`. All rules, workflow, work units, profiles, and token discipline
are defined in `AGENTS.md`.

Commands are the neutral playbooks in `commands/`, exposed here as slash commands via
`.claude/commands/` wrappers.
