/**
 * Placeholder: nunca se sirve mientras `HABILITADO` sea `false` en el layout
 * (`notFound()` corta antes de renderizar). Existe para que la ruta `/hq` sea
 * comprobable por prueba (FU-06, criterio 5) sin esperar a DU-13.
 */
export default function HqPlaceholder() {
  return null;
}
