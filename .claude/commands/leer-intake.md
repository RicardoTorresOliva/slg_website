---
description: Lee la respuesta de un cliente al intake (hoja + carpeta de Drive) y produce su site.config.ts, los borradores y los pendientes.
---

Follow the neutral playbook in `commands/leer-intake.md`. Do not restate it here.

(Wrapper so the command appears in Claude Code's slash menu. The same `commands/leer-intake.md` is used
unchanged by Cursor, Antigravity, and any other platform.)
