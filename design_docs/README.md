# design_docs/

Design documents are **profile-dependent**: the active Asset Profile declares which documents this
asset requires and at what detail level (HIGH / MEDIUM / LIGHT). They are produced by `init-project`
(Step 6). This folder ships empty by design.
