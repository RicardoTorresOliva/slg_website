---
description: APP_Builder governance pointer
alwaysApply: true
---

# APP_Builder (Cursor adapter)

This project is governed by `AGENTS.md` at the repository root. Read and follow it for all rules,
workflow, work units (Foundation Units / Deliverable Units), the active Asset Profile, and token
discipline. This file only points; it never restates governance.

Commands are the neutral playbooks in `commands/`. Invoke them by reading the relevant playbook
file and following it.
