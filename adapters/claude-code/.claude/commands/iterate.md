---
description: Modo iteracion post-entrega: aplica cambios, mejoras o corrige bugs (con spec-delta).
---

Follow the neutral playbook in `commands/iterate.md`. Do not restate it here.

(Wrapper so the command appears in Claude Code's slash menu. The same `commands/iterate.md` is used
unchanged by Cursor, Antigravity, and any other platform.)
