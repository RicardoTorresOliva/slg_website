---
description: Inicializa un proyecto nuevo. Selecciona perfil, explora stack, planifica (sin producir).
---

Follow the neutral playbook in `commands/init-project.md`. Do not restate it here.

(This wrapper exists only so the command appears in Claude Code's slash menu. The playbook body
is produced in milestone M1. The same `commands/init-project.md` is used unchanged by Cursor,
Antigravity, and any other platform.)
