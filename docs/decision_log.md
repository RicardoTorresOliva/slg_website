---
type: docs
title: Decision log
project: slg_website
status: planning
timestamp: 2026-09-08
---

# Decision log — slg_website

Decisiones significativas. Las 13 decisiones HITL previas de Ricardo viven en `START_PROJECT.md` §10
y **no se re-exploran**; aquí se registran desde D-14 las que toma la planificación.

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-14 | 2026-09-08 | **Perfil de activo: `software-app`** | `intelligence-product` (compuesto) | El activo es una aplicación desplegable con superficie de administración. Los gates extra (marketing, identidad, API) se declaran en el brief (Anexo D), no exigen perfil compuesto. Regla del perfil sobre superficie de admin/operaciones: aplica (HQ lo es). |
| D-15 | 2026-09-08 | **Correo transaccional: servicio transaccional dedicado** con dominio verificado (SPF/DKIM/DMARC). Producto concreto pendiente de elegir en el paso 5 con 2–3 candidatos. | SMTP de Google Workspace tras la migración | Entregabilidad medible, webhooks de rebote y logs, que un buzón corporativo no da. **Corrección posterior (D-23)**: la alternativa de Google Workspace queda además sin objeto, porque el correo se queda en Microsoft 365 y no hay migración. La categoría se sostiene igual: enviar correo automatizado desde un buzón de persona produce entregabilidad mala, topes de envío y ninguna prueba de entrega. |
| D-16 | 2026-09-08 | **Anti-abuso sin desafío de terceros en v1**: límite de peticiones + honeypot + lista de dominios de correo gratuito, todo propio. | Desafío anti-bot de terceros | Cero scripts de terceros en la capa pública: protege el gate D1 (Lighthouse ≥ 90, JS inicial < 150 KB gz) y la promesa privacy-first. Reversible: se añade si aparece spam real. |
| D-17 | 2026-09-08 | **11 documentos de descarga** (D-01…D-11), uno por página de servicio, incluidos D-04 (Customize Programs) y D-05 (AI Coaching for Directors). Resuelve la contradicción entre §10-12 ("9") y A.2/A.4 ("11"). | 9 documentos; 11 registros lanzando con los que existan | El contrato de página A.3 §5 exige la descarga como **CTA único** de cada página de servicio. Con 9, dos páginas quedarían sin CTA y necesitarían un contrato alternativo. Los PDFs se producen en SLG_Overhauling; mientras falten, la página muestra "disponible próximamente" y captura el email igual (mecanismo ya previsto en A.4). |
| D-18 | 2026-09-08 | **MCP del CRM registrado con `--scope local`** (`~/.claude.json`), nunca `--scope project`. | `--scope project` (`.mcp.json` en el repo); `--scope user` | El repo `slg_website` es **público** (§10-6) y `.gitignore` no cubre `.mcp.json`: el scope de proyecto habría escrito credenciales dentro del repositorio. El scope local mantiene el secreto fuera del control de versiones. Verificado: `git status` limpio tras el alta. |
| D-19 | 2026-09-08 | **Entrega de capturas al CRM: adaptador de dos modos**, seleccionable por variable de entorno. Modo `contact_note` (lo que el CRM permite hoy: `POST /contacts` + `POST /notes`) y modo `lead_admission` (`POST /api/v1/leads` con alcance `leads:write`, cuando exista). | Solo contacto+nota en v1; ejecutar el `/iterate` del CRM antes de M2 | M2 no queda bloqueado por un cambio en otro repositorio (`~/Dev/crm_slg`). El spec-delta del CRM se enchufa después sin reabrir el DU de captura ni migrar datos. Coste: el `api_contracts` debe especificar los dos modos y el DU de captura probar ambos. |
| D-20 | 2026-09-08 | **Backups a object storage S3-compatible externo, en proveedor distinto de Hostinger, con tramo gratuito.** Producto concreto pendiente de elegir en el paso 5 con 2–3 candidatos. | Google Drive de SLG; segundo VPS o NAS propio | El riesgo "un solo VPS" (§9) no se mitiga si el backup vive en la máquina que puede morir. El tramo gratuito mantiene el coste en cero mientras el volumen v1 sea pequeño. El DoD #8 exige restauración probada, que es más fiable sobre API S3 que sobre Drive. |
| D-21 | 2026-09-08 | **Destino de backups: Cloudflare R2.** Resuelve P-2 dentro de la categoría fijada en D-20 (object storage S3-compatible externo, proveedor distinto de Hostinger). | Backblaze B2 (recomendación del análisis, por Object Lock); IDrive e2 | Elección de Ricardo (AGENTS.md Regla 7). R2 da **egreso $0 a cualquier volumen, sin franquicia**: restaurar nunca genera factura, que es justo cuando hay una emergencia. Tramo gratuito 10 GB-mes + 1M operaciones clase A + 10M clase B, recurrente. **Contrapartidas asumidas**: (a) sin Object Lock por API estándar, la protección contra borrado malicioso hay que resolverla de otro modo — ver R-nuevo en `planning/risks.md`; (b) las operaciones son la partida medida: el cliente de backup debe configurarse para no dispararlas. El script de backup y restauración se escribe contra API S3 genérica: cambiar de proveedor es endpoint + credenciales en variables de entorno. |
| D-22 | 2026-09-08 | **Correo transaccional: Resend.** Resuelve P-1 dentro de la categoría fijada en D-15. | SMTP2GO (suplente inmediato); Zoho ZeptoMail; descartados con motivo: Brevo (estampa su logo en el plan gratuito), Mailgun (gratis solo a 5 destinatarios autorizados), Amazon SES (sin tramo gratuito para clientes nuevos), Postmark y MailerSend (tramos demasiado cortos) | Elección de Ricardo (AGENTS.md Regla 7). 3.000 correos/mes gratis frente a los 1.000 del suplente, retención de registros de 30 días frente a 5, sin marca de terceros en los correos, y SMTP estándar disponible como vía de escape. **Condición de diseño**: el DU de correo se construye como adaptador tras variable de entorno y habla SMTP, no el SDK propietario — así cambiar de proveedor cuesta tres variables de entorno y ninguna línea de código. **Aviso privacy-first**: el seguimiento de aperturas y clics debe quedar DESACTIVADO por dominio. |
| D-23 | 2026-09-08 | **El correo corporativo de SLG permanece en Microsoft 365.** No hay migración a Google Workspace. Los MX de Outlook se mantienen. | Migrar a Google Workspace (supuesto del brief §7 y §1 Constraints, ahora descartado) | Decisión de Ricardo. **Efectos sobre el plan**: (a) desaparece la urgencia de F.2-3 — no se va a cancelar M365, así que el registro en Entra ya no compite contra una fecha de cancelación, y **puede reutilizarse el tenant existente**; (b) el registro SPF de la raíz debe llevar el `include` de Resend **junto al de Outlook**, sin romperlo — es la operación delicada que sustituye al riesgo de la migración; (c) el MX de Outlook entra formalmente en la lista de "no tocar" del §7, junto a `crm`, `n8n`, `evolution` y `academy`. |
| D-24 | 2026-09-08 | **Subdominio de envío dedicado para el correo transaccional.** Resend verifica un subdominio propio (p. ej. `mail.softlandingglobal.com`); el registro SPF de la raíz **no se toca** y sigue autorizando solo a Outlook. Resuelve el `[PENDIENTE]` abierto en R-38. | Convivencia de dos `include` en el SPF de la raíz (opción descartada) | Elección de Ricardo. Aísla por completo la reputación de envío automatizado de la del correo humano: un problema de entregabilidad en uno **no puede** arrastrar al otro, y publicar los registros del subdominio no puede romper el correo corporativo, porque son entradas DNS distintas. **Contrapartida asumida**: la dirección remitente deja de ser exactamente la que fija el brief (`support@softlandingglobal.com`) — ver la sub-decisión pendiente sobre el `From`. `[PENDIENTE: nombre exacto del subdominio]`, se fija en M0. |

## Decisiones tomadas por los `design_docs` (D-25…D-42)

Las **24 declaraciones** que los cinco `design_docs` hicieron por su cuenta —porque el brief no las
fijaba—, agrupadas en **18 entradas** tal como las consolida `design_docs/design_summary.md` §1.
Origen: `data_model` §11.2 (5), `api_contracts` §13.3 (12), `architecture` §15.3 (7) y la regla
derivada de `style_guide` §2.3. Nada aquí es nuevo: se transcribe lo ya escrito.

> **Marca de atención.** Las filas marcadas **[IRREVERSIBLE-TRAS-FU-04]** son las que dejan de ser
> gratuitas en cuanto corre la primera migración (FU-04, unidad de M0-A): revertirlas después es
> migración de datos, no una edición. Son las que Ricardo debe mirar con más atención antes de
> aprobar el bloque.

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-25 | 2026-09-08 | **Tamaño máximo de subida por destino** (`data_model` §2.6): **25 MB** en `downloads`, **50 MB** en `deliverables` (`pdf`/`material`), **5 MB** en `html`, **1 MB** en `md`, **tope duro 50 MB**. | Otros topes: RNF-25 dejó el número abierto y mandó fijarlo aquí | Cierra el `[PENDIENTE]` de RNF-25. Impacto de revertir: bajo si sube, **alto si baja** — FU-04 y FU-09 usan la constante única, y el presupuesto del tramo gratuito de R2 (10 GB-mes) está calculado sobre estos topes. |
| D-26 | 2026-09-08 | **Enumerados de estado deliberadamente mínimos** (`data_model` §3.3): `organization.status` con **2** valores (`active`, `archived`) y `project.status` con **3** (`active`, `completed`, `archived`). **[IRREVERSIBLE-TRAS-FU-04]** | Añadir estados comerciales (`paused`, `negotiation`) | B.2, RF-77 y RF-79 piden «estado» sin enumerarlo. Los estados comerciales mueven la frontera (a) de `scope.md`: eso es el CRM, no la web. **Cambiar el enumerado después de FU-04 es migración de datos.** |
| D-27 | 2026-09-08 | **Convenciones de esquema en la frontera con Better Auth** (`data_model` §1.2, §2.1, §2.3): **PK `text` uniforme** en las 19 tablas, **enumerados como `text` + `CHECK` nombrado**, renombres **`member`→`membership`** y **`apikey`→`api_key`**, alcances en columna propia `scopes`, `timestamptz` en todas las fechas. *(Agrupa 3 de las 5 declaraciones de §11.2.)* **[IRREVERSIBLE-TRAS-FU-04]** | Tipo `ENUM` nativo de Postgres; conservar los nombres de Better Auth (`member`, `apikey`) | El brief no entra en el tipo de las claves ni en cómo se escriben los enumerados. **Muy alto después de la primera migración: es el tipo de la clave primaria de las 19 tablas. Prácticamente irreversible una vez corre FU-04.** |
| D-28 | 2026-09-08 | **Caducidad de las URLs firmadas** (`api_contracts` §11.9): **15 min** descarga pública · **10 min** entregable del portal · **30 min** subida por API. Tres **variables de entorno**. | Tres constantes en código en vez de tres variables de entorno | Cierra el `[PENDIENTE]` de RNF-20. Impacto de revertir: bajo, son variables de entorno — pero alargarlas debilita el gate D10 y acortarlas rompe la descarga desde móvil con mala cobertura (DoD #1). |
| D-29 | 2026-09-08 | **Forma transversal de `/api/v1`** (`api_contracts` §2.1, §2.5, §2.7, §3.9): **sobre de error único** (`code` estable, `message` sin internos, `request_id` = `audit_log.id`), **paginación por cursor opaco** (50 por defecto, 200 máximo), **sin `PATCH`, `PUT` ni `DELETE`**, y `openapi.json` **generado** de los mismos esquemas que validan. *(Agrupa 4 de las 12 de §13.3.)* | — B.5 pide una API v1 con alcances; no fija forma de error, paginación, verbos ni cómo se produce la especificación | **Alto una vez publicada**: cambiar la forma del error o la paginación rompe a todo consumidor. Antes de M5 el coste es cero. |
| D-30 | 2026-09-08 | **En la cola del CRM, las cinco esperas preceden a los cinco intentos** (1 min → 10 min → 1 h → 6 h → 24 h) (`api_contracts` §8.1). **Resuelve C-1.** | Primer intento inmediato — obliga a descartar la espera de 24 h o a subir el tope a 6 intentos, lo que toca la restricción `crm_delivery_attempt_bounded` | B.6-3 y RF-50 enumeran cinco esperas; el esquema acota a cinco intentos. Solo encajan si cada espera precede a un intento. **Coste asumido**: el contacto llega al CRM ~1 min después de la captura. |
| D-31 | 2026-09-08 | **Visibilidad por fila de las claves de API** (`api_contracts` §3.6): una clave **acotada a una empresa** ve solo entregables `client` **publicados**; una **clave de SLG** lo ve todo. | — B.5 fija alcances por verbo, no visibilidad por fila | **Alto**: es la regla que sostiene el aislamiento en la superficie de agentes (DoD #5, gate D9). Relajarla filtra borradores de una empresa a otra. |
| D-32 | 2026-09-08 | **Contrato criptográfico y de garantía de los webhooks** (`api_contracts` §9.3, §9.4 + `architecture` A-07): cabecera **`X-SLG-Signature`**, cadena firmada **`<timestamp>.<cuerpo crudo>`**, tolerancia de **5 min**, **dedupe por `X-SLG-Delivery`**, entrega **«al menos una vez»**. *(Agrupa las dos: son la misma decisión.)* | — B.7 exige «firma HMAC-SHA256 en cabecera» y no fija nombre, cadena, tolerancia ni garantía de entrega | **Alto tras el primer suscriptor**: cambiar la cadena firmada invalida toda verificación existente. Antes, cero. |
| D-33 | 2026-09-08 | **Los webhooks no transportan datos personales** (`api_contracts` §10.1, §10.3, §10.4, §10.6, §10.8): ni correo, ni nombre, ni token, ni cuerpo de aviso, ni URL firmada — solo identificadores y el hecho. | Transportar en la carga útil correo, nombre, token, cuerpo de aviso o URL firmada | El brief lista los nueve eventos, no su carga útil. Bajo técnicamente, **alto en promesa**: es privacy-first aplicado. Revertirlo saca datos personales a un canal saliente. |
| D-34 | 2026-09-08 | **`publish` es obligatorio y sin valor por defecto** en `POST /announcements` (`api_contracts` §3.7). | `publish` con valor por defecto implícito | B.5 no dice si publicar es el defecto. Un defecto implícito publica avisos a clientes por omisión: el fallo es **silencioso y visible para el cliente**. |
| D-35 | 2026-09-08 | **Plantillas exactas de la nota del CRM**, una por `source`, con las siete reglas de composición (`api_contracts` §6.3). | — B.6-2 da un ejemplo de nota, no un contrato | Bajo: cambiar la plantilla no migra nada. Pero las notas ya escritas en el CRM quedan con el formato viejo. |
| D-36 | 2026-09-08 | **El adaptador de correo habla SMTP estándar** (`api_contracts` §11.3): **no existe variable de SDK propietario**, y esa ausencia es la decisión. Materializa la condición de diseño que **D-22** ya impone. *(`design_summary` §1 propone anotarlo bajo D-22 en vez de darle ID propio; se registra como fila con el puntero.)* | Variable de configuración de un SDK propietario del proveedor | D-22 lo exige como condición; ningún documento lo había materializado en variables. Bajo hoy, **alto mañana**: es lo que hace que cambiar de proveedor cueste tres variables y ninguna línea de código. |
| D-37 | 2026-09-08 | **Ejecución de colas dentro del servicio `slg-web`** (`architecture` §6.2, §6.3): el ejecutor **no** vive en un servicio aparte ni en un orquestador externo (A-01), y reclama trabajo por **reserva con plazo empujando `next_attempt_at`**, sin estado «en curso» nuevo (A-02). *(Agrupa A-01 y A-02.)* | Servicio trabajador aparte u orquestador externo; estado «en curso» nuevo en el esquema | El brief no dice dónde corre el trabajo en segundo plano; RF-115 solo exige que n8n sea opcional. Medio: separar el trabajador es la salida de escape ya documentada (§6.7) y no reabre el esquema; cambiar el mecanismo de reserva sí toca las tres colas a la vez. |
| D-38 | 2026-09-08 | **404, nunca 403**, al cruzar de superficie o de empresa (`architecture` A-03 + `ui_wireframes` §1.4, §8.1). *(Agrupa: los dos documentos dicen lo mismo.)* | Devolver 403 — RF-71 dice literalmente «404/403» y deja la ambigüedad abierta | Bajo de construir, **alto de seguridad**: un 403 confirma que la ruta o el recurso existen. Coherente con los mensajes neutros de RF-59. |
| D-39 | 2026-09-08 | **En `/api/v1`, el límite de peticiones (429) se comprueba antes que el alcance (403)** (`architecture` A-04, §2.5). | Comprobar el alcance (403) antes que el límite (429) | El brief fija ambos controles, no su orden. Bajo: es una línea de middleware. Invertirlo deja que una clave martillee endpoints prohibidos sin consumir cuota. |
| D-40 | 2026-09-08 | **La caché de métricas del CRM vive en memoria del proceso**, no en una tabla (`architecture` A-05, §3.6). | Caché persistida en tabla | RF-55 exige caché de 5 minutos y no dice dónde. Bajo: un reinicio cuesta una lectura. Llevarla a tabla obliga a declararla en `data_model`. |
| D-41 | 2026-09-08 | **El espejo de la tabla `download` se sincroniza en el despliegue y nunca borra filas** (`architecture` A-06, §5.3). | Borrar filas del espejo al retirar un documento | B.4 define el puente contenido↔base de datos sin decir qué pasa al retirar un documento. **Alto**: borrar filas rompe la trazabilidad de las capturas anteriores, que apuntan a `download` como ancla de identidad. |
| D-42 | 2026-09-08 | **Regla derivada de los contrastes medidos** (`style_guide` §2.3): si una sección oscura necesita cyan legible, la sección es **`--indigo`, no `--blue-deep`** (2,8:1 vs 5,3:1); y **`--line` divide pero no delimita** un control (1,6:1). *(`design_summary` §1 la marca `[propuesta]`: el documento no la declara como decisión a registrar.)* | Mantener `--blue-deep` con cyan encima; usar `--line` como delimitador de control — el Anexo C admite el cyan como acento en secciones oscuras sin medir el contraste sobre cada fondo | Los números están medidos sobre los hexadecimales del propio kit. Bajo de construir, pero revertirlo es publicar texto que **falla AA**: choca de frente con el gate D2 y RNF-04. |
| D-43 | 2026-09-08 | **Monitorización externa: servicio de uptime dedicado con tramo gratuito, ejecutado fuera del VPS.** Cierra P-5, RF-130 y el gate D11; resuelve R-29. Producto concreto pendiente de elegir con 2–3 candidatos (paso 5). | n8n propio (lo sugiere §9 del brief); Cloudflare, ya presente por D-21; aplazar a v1.1 relajando D11 | Elección de Ricardo. n8n corre en el **mismo VPS** que debería vigilar (`167.88.42.76`): si el servidor cae, el monitor cae con él y nadie se entera — sirve como señal **secundaria**, nunca como principal. Concentrar backups y monitorización en un mismo proveedor reduciría la independencia que motivó D-20. |
| D-44 | 2026-09-08 | **Anillo de foco de dos capas**: capa exterior `--cyan` (#50B4DC), capa interior `--blue-primary` (#2878B4) o `--ink`. Resuelve el conflicto CF-3. | Anillo sólido en `--blue-primary`; mantener `--cyan` sólido como lista el Anexo C.1 | Elección de Ricardo. El Anexo C.1 lista `--cyan` entre los usos de anillo de foco, pero medido da **2,4:1 sobre `--paper`**: no cumple RNF-05 ni el gate D2, y el DoD #7 exige Lighthouse Accessibility ≥ 90. Las dos capas **conservan la intención cromática del kit** y añaden el contraste que le faltaba: es una precisión del kit, no una contradicción. Se fija el token en FU-02 y se verifica en FU-10. |
| D-45 | 2026-09-08 | **El visor de entregables HTML se sirve desde un ORIGEN SEPARADO** (subdominio propio), como norma. Resuelve el conflicto CF-4 en favor de lo que `data_model` §3.10 y `ui_wireframes` §7.3 ya prometían. El `iframe sandbox` sin `allow-same-origin` y la CSP estricta **se mantienen**: son defensa en profundidad, no la alternativa. | Solo `iframe sandbox` + CSP, como especificaba `architecture` §11.3 | Elección de Ricardo. Parte de los entregables HTML los generan **agentes Hermes**: con origen separado, un script hostil dentro del entregable no puede leer cookies de sesión ni datos de la aplicación, porque el navegador lo aísla por política de origen. Con solo sandbox, la protección depende de que la configuración sea correcta **siempre**; un fallo de configuración expone la sesión del cliente. Se construye en DU-19. `[PENDIENTE: nombre del subdominio del visor, se fija en M4]`. |
| D-46 | 2026-09-08 | **Hermes Agent asume, por cron, las compuertas de revisión que no exigen a Ricardo.** Mitigación de R-16 (aprobador único). Alcance: ejecutar `/review` por milestone con contexto limpio, correr las pruebas E2E del DoD en contenedor y devolver veredicto estructurado, y recordar las compuertas pendientes con su paquete de decisión ya preparado. **Fuera de su alcance, por gobernanza: el Planning Gate (Regla 1), la compuerta de copy FU-01 y las elecciones de producto (Regla 7)** — esas siguen siendo de Ricardo. | Mantener a Ricardo como único aprobador de todo; construir un validador dentro de slg_website | Propuesta de Ricardo. R-16 tenía probabilidad **Alta** e impacto **Alto**: el proyecto se detiene esperando decisiones de una sola persona. `AGENTS.md` solo exige que la revisión por milestone la haga *"un revisor independiente con contexto limpio"* — no dice que sea Ricardo, así que delegarla **no viola el método**. **Sin cambio de alcance en este repositorio**: el cron vive en Hermes Agent y consume lo que este proyecto ya produce — el pipeline de CI de FU-05 y, tras M5, `POST /api/v1/events` con `agent_event`. §5.3 mantiene «Hermes como validador» en *previsto, no se construye*: esta decisión **no lo mueve a alcance**, solo confirma quién opera la compuerta desde fuera. |

## Decisiones de ejecución (D-47…D-51)

Las que tomaron las unidades ya construidas. **D-47, D-48 y D-49 estaban citadas en
`implementation/task_tracker.md`, en `docs/work_log.md` y en el propio esquema sin tener entrada
aquí**: esta sección cierra ese hueco (AGENTS.md Regla 6). D-50 y D-51 se registran ahora, con su
unidad.

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-47 | 2026-09-08 | **El rol que sirve peticiones deja de ser el dueño del esquema** (FU-04, migración `0002`): `slg` migra, `slg_app` sirve —sin `BYPASSRLS` y sin ser dueño—, con **dos cadenas de conexión distintas** (`DATABASE_URL_MIGRATIONS` y `DATABASE_URL`). | Un solo rol para todo, que es lo que la imagen de PostgreSQL da por defecto | **Hallazgo grave dentro de FU-04**: el usuario que crea la imagen es superusuario y lleva `rolbypassrls`. Con él **las políticas de fila no se aplican**: una consulta sin contexto devolvía las dos empresas en vez de ninguna, y todo el aislamiento de la migración `0001` habría sido decorativo. La primera comprobación de `test:isolation` vigila exactamente esto: si alguien vuelve a apuntar `DATABASE_URL` al dueño, el CI se pone rojo. **Efecto operativo**: `slg_app` nace con `LOGIN` y **sin contraseña**; la pone quien despliega (`docs/deployment.md` §2.1), nunca el repositorio, que es público. |
| D-48 | 2026-09-08 | **`contact` es una entidad propia, distinta de `user`**, con etiqueta booleana de contacto principal (`is_primary`) y un índice único que permite **uno solo por empresa**; `user_id` se rellena solo si esa persona acaba teniendo cuenta. **Cierra H-04.** | `organization.primary_contact` como texto libre; o como `FK` a `user`, que obligaría a dar cuenta de portal a todo contacto comercial | H-04 preguntaba si el contacto principal era usuario con `FK` o texto libre, y las dos respuestas eran malas: el texto libre no se puede enlazar, y la `FK` a `user` obliga a crear cuentas a gente que nunca entrará al portal. La tabla propia admite **el contacto sin cuenta**, que es el caso normal antes de la invitación, y lo enlaza cuando la acepta. **Frontera que se defiende**: aquí no entra estado comercial —ni etapa, ni importe, ni probabilidad, ni próximo paso—; eso es pipeline y vive en el CRM. |
| D-49 | 2026-09-08 | **Monitorización externa: UptimeRobot.** Resuelve **EXT-7** dentro de la categoría que cerró D-43. Y, en la misma sesión, **S-01 se difiere a go-live**. | Better Stack (mejor panel, tramo gratuito más corto); Healthchecks.io (orientado a cron, no a HTTP público) | Elección de Ricardo (Regla 7). Tramo gratuito de 50 monitores a 5 minutos, corriendo **fuera del VPS**, con correo y push móvil como canales de aviso —ninguno de los dos depende de `167.88.42.76`—, que es la condición que D-43 impuso. n8n queda como señal **secundaria**: vive en la misma máquina que debería vigilar. **S-01 diferida**: la credencial afectada es de **construcción**, no de producción; las de producción se crean nuevas y **nunca** se reutiliza la afectada en un entorno desplegado. La rotación sigue siendo requisito de go-live. Con esto **FU-05 queda desbloqueada**. |
| D-50 | 2026-09-08 | **`crm_delivery` lleva columna `cycle`**, y la unicidad del intento es `(lead_capture_id, cycle, attempt, endpoint)`. El reintento manual de RF-52 sobre una captura `failed` **abre un ciclo nuevo** en vez de superar el tope de cinco intentos. **Resuelve CF-1.** | Subir el tope de intentos por encima de cinco; permitir intentos fuera de la restricción `crm_delivery_attempt_bounded` | Decisión tomada dentro de FU-04, ya construida en el esquema, pero sin entrada aquí. `api_contracts` §13.2 declaraba CF-1 bloqueante de **DU-16**: el conflicto era que el reintento manual de HQ no cabía en un tope de cinco intentos fijado por B.6. El ciclo lo resuelve **sin tocar el backoff** y sin migrar datos. **Efecto en el plan**: DU-16 ya no espera un spec-delta del `data_model`; la vía está construida y verificada. |
| D-51 | 2026-09-12 | **Tres precisiones de la compuerta de staging y del presupuesto de JS** (FU-05): (a) la compuerta se activa por la **presencia** de `STAGING_BASIC_AUTH_USER`/`PASSWORD`, no por `NODE_ENV` ni por el nombre del host; (b) **`/api/health` queda fuera** de la compuerta; (c) el presupuesto de JS **no cuenta los polyfills `noModule`**. | (a) activar por `NODE_ENV` o por host; (b) cerrar también la sonda; (c) contar todos los `<script>` del HTML | (a) Un despliegue nuevo que olvide las variables queda **abierto y se nota**, en vez de medio protegido por una heurística de host; y producción no paga nada porque no las define. (b) UptimeRobot no lleva credenciales: una sonda tras `401` mediría la compuerta, no el servicio, y la sonda no devuelve ningún dato de negocio. (c) Un navegador moderno **ignora** el paquete `noModule`: no lo pide ni lo ejecuta. Contarlo gastaba 39 KB del presupuesto en bytes que el público objetivo no descarga, y el gate D1 se mide sobre el navegador moderno (Lighthouse). Las tres quedan verificadas por `npm run check:runtime` y `npm run check:js-budget`. |

## Decisiones de FU-06 (D-52…D-54)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-52 | 2026-09-12 | **No se usa el plugin `apiKey` de Better Auth.** Se usan `organization` y `admin`; la verificación de claves de `/api/v1` la hace `lib/auth/api-key.ts`, dentro del mismo módulo, contra la tabla `api_key` que `data_model` §3.6 ya especifica. **Desviación declarada** respecto al texto de FU-06, que nombraba los tres plugins. | Adoptar el plugin y doblar nuestra tabla a su forma (21 columnas, `permissions` como cadena opaca, `referenceId` obligatorio) | Tres razones, y la tercera es la que decide. (a) El plugin exige `referenceId` **obligatorio**: nuestras claves de SLG **no tienen empresa** (`api_key.organization_id` es nullable por B.3), así que serían imposibles. (b) Guarda los permisos como cadena opaca; `data_model` §3.6 fija seis alcances en `jsonb` con un CHECK de contención que impide **en la base** guardar un alcance inventado — adoptar el plugin cambiaría una garantía de base de datos por una de librería. (c) FU-06 existe para que sustituir el proveedor de identidad no toque nada más (R-19): meter la verificación de claves dentro del proveedor va justo en contra de la unidad que la pide. **Coste asumido**: el límite por clave y el hasheo los escribimos nosotros, ~150 líneas, probadas contra PostgreSQL real. |
| D-53 | 2026-09-12 | **La pertenencia y la clave de API se resuelven por dos funciones `SECURITY DEFINER` estrechas** (`app_memberships_de_usuario`, `app_clave_api_por_hash`), no relajando las políticas de fila. Cada una contesta una pregunta, no acepta filtros, lleva `search_path` fijado y solo tiene `EXECUTE` el rol de aplicación. | (a) Añadir `'system'` a la política de las ocho tablas con `organization_id`; (b) conectar el módulo de identidad con el rol dueño | El inicio de sesión y la verificación de una clave ocurren **antes** de saber la empresa —esa es la pregunta—, así que no hay contexto que fijar y `membership` y `api_key`, bajo RLS forzada, devuelven cero. (a) abriría de golpe `project`, `deliverable`, `announcement` y `contact` a cualquier código que llame a `withSystemScope`: el agujero sería ocho veces mayor que el problema. (b) es repetir el hallazgo de FU-04 —el dueño lleva `BYPASSRLS` y apaga el aislamiento entero—. Las funciones son auditables en una línea de `\df+` y su llamante único lo vigila `check:auth-boundary`. |
| D-54 | 2026-09-12 | **`invitation.token_hash` pasa a opcional** (migración 0005), conservando su índice único. | Mantenerla obligatoria y que el plugin `organization` no pueda crear invitaciones | Dos flujos escriben en `invitation` y no escriben lo mismo: el plugin crea la fila y usa el id como referencia; el servicio de FU-07 genera el enlace firmado del correo y guarda su hash. Con la columna obligatoria, el adaptador avisa de que «inserts into invitation will fail». Se vuelve opcional porque **lo es**: una invitación existe antes de tener enlace enviado. **No se relaja nada más**: en PostgreSQL un índice único admite varios NULL, así que dos enlaces iguales siguen siendo imposibles. |

## Decisiones de FU-08 (D-55…D-56) y propuesta de P-3/P-4

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-55 | 2026-09-12 | **Desaparece `RESEND_API_KEY` de `.env.example`; las variables de correo son de TRANSPORTE, no de marca**: `MAIL_SMTP_HOST`, `MAIL_SMTP_PORT`, `MAIL_SMTP_USERNAME`, `MAIL_SMTP_PASSWORD`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME`, `MAIL_REPLY_TO`, `MAIL_ALERTS_TO`. | Mantener una variable con el nombre del producto junto al bloque SMTP | `api_contracts` §11.3 lo dice con todas las letras: «**no hay `RESEND_API_KEY` como tal**, y esa ausencia es la decisión». `.env.example` la traía desde FU-02 y contradecía a D-36. El proveedor elegido se configura poniendo su servidor en `MAIL_SMTP_HOST` y su clave de API en `MAIL_SMTP_PASSWORD`. Verificado en la práctica: la suite corre entera contra **dos servidores SMTP distintos** cambiando solo variables (criterio 2). |
| D-56 | 2026-09-12 | **Los correos con enlace (`invitation`, `password_reset`) NO los reintenta el barrendero**: quedan `failed` desde el primer fallo, con el motivo escrito. Reintentar significa **volver a emitir**, y eso lo hace FU-07 o DU-01 con un token nuevo. Los dos avisos internos sí se recomponen desde `lead_capture` y sí se reintentan. | Guardar los datos de plantilla —incluido el token— en `email_delivery` para poder reenviar el mismo correo | `architecture` §8.2 prohíbe que el puerto lleve a la base el cuerpo del correo y los tokens. Guardarlos convertiría la tabla de correo en un **almacén de credenciales activas**: cualquiera con lectura sobre `email_delivery` tendría enlaces de acceso vigentes. El coste asumido es que una invitación fallida exige reemitir, que además es lo correcto: el token original ya arrastra su caducidad. |

### P-3 y P-4 — **CERRADAS** (Ricardo, 2026-09-12)

| Sub-decisión | Valor fijado |
|---|---|
| **P-4** · subdominio de envío dedicado | **`mailweb.softlandingglobal.com`** |
| **P-3** · dirección remitente (`From`) | **`no-reply@mailweb.softlandingglobal.com`** |
| `Reply-To` (RF-117) | **`support@softlandingglobal.com`** — toda respuesta del destinatario sigue llegando ahí |
| Destinatario de avisos (RF-53, RF-50) | **`support@softlandingglobal.com`** |

Son **valores de variable de entorno**, no constantes de código: viven en
`MAIL_FROM_ADDRESS`, `MAIL_REPLY_TO` y `MAIL_ALERTS_TO`, y se persisten en cada envío
(`email_delivery.from_email`, `.reply_to`). **El brief §5.1 y RF-117 quedan desactualizados** donde
dicen que el remitente es `support@softlandingglobal.com`: desde D-24 eso es el `Reply-To`.

**Corrección de la propuesta, 2026-09-12.** Yo había propuesto `mail.softlandingglobal.com`. El
subdominio real es **`mailweb.`**, creado y **verificado por Ricardo el 2026-09-11** en la región
**São Paulo (`sa-east-1`)** sobre DNS de Hostinger. `mailweb` distingue mejor: deja `mail.` libre por
si algún día hace falta para otra cosa y dice en el propio nombre que el remitente es la **web**, no
una persona. Manda el valor real, no la propuesta.

Con esto, **EXT-6 queda cerrada** y FU-08 pierde su última condición de entrada abierta.

## Decisiones de FU-07 (D-57…D-59)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-57 | 2026-09-12 | **Reenviar una invitación emite un testigo NUEVO y renueva la caducidad**, sobre la misma fila. No se reenvía el enlace anterior. | Reenviar el mismo enlace, conservando su caducidad original | Dos motivos. (a) El enlace viejo arrastra su caducidad: una invitación reenviada en la hora 71 duraría un minuto, y quien la recibe no entiende por qué. (b) Si el primer correo llegó a un buzón equivocado —un alias, un reenvío automático, una dirección mal escrita—, reenviar el mismo enlace deja **ese** enlace vivo. Emitir uno nuevo invalida el viejo, y el índice único sobre `token_hash` garantiza que solo uno sirva. |
| D-58 | 2026-09-12 | **`NEXT_PUBLIC_SITE_URL` es la única URL base de la instancia.** El `APP_BASE_URL` que nombra `api_contracts` §11.1 es esa misma variable con el nombre que el proyecto usa desde FU-02. | Añadir `APP_BASE_URL` como variable propia para los enlaces absolutos | Dos nombres para una cosa es exactamente cómo un enlace de staging acaba en un correo de producción: alguien rellena uno y olvida el otro, y los dos parecen correctos. Una sola variable, comprobada por `check:env`, que además falla si el código lee una que no está declarada. |
| D-59 | 2026-09-12 | **`membership.org_role` guarda el rol de B.3** (`slg_admin`, `slg_operator`, `client_admin`, `client_member`), no el vocabulario `admin`/`member` del plugin. | Traducir al vocabulario del plugin al escribir la pertenencia | **La base ya lo había decidido y el documento no se había enterado**: la migración `0000` fijó el `CHECK` sobre los cuatro roles de B.3 y el defecto en `client_member`; `data_model` §3.4 describe el vocabulario del plugin. Escribir `'admin'` viola la restricción — lo descubrió la prueba de FU-07 al canjear la primera invitación. Se conserva lo que la base impone, que además evita una traducción en cada lectura. **Consecuencia registrada**: los endpoints de pertenencia **del propio plugin** escribirían `owner`/`admin`/`member` y fallarían; no se usan —FU-07 es quien emite y canjea—, y el día que se usen necesitarán un mapeo explícito. |

## Decisiones de FU-09 (D-60…D-61)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-60 | 2026-09-12 | **Las tres caducidades de URL firmada pasan a variables de entorno** (`SIGNED_URL_TTL_DOWNLOAD_MINUTES`, `SIGNED_URL_TTL_DELIVERABLE_MINUTES`, `SIGNED_URL_TTL_UPLOAD_MINUTES`), con los defectos en `lib/db/limits.ts` y un **tope duro de 60 minutos**. Y se corrige que **`upload` y `deliverable` estaban intercambiados**. | Dejarlas como constantes, que es como estaban desde FU-04 | El criterio 3 de FU-09 exige que el valor **se lea de configuración**, y §11.9 lo dice con todas las letras: «por eso son tres variables y no tres constantes». **La corrección no es cosmética**: con los valores intercambiados, un entregable del portal habría vivido **30 minutos** —el triple de lo especificado, debilitando el gate D10— y una subida de 50 MB habría tenido **10**, que por una conexión mala no bastan (y esa es justo la que atraviesa una transferencia real). El tope de 60 min se añade porque una variable mal puesta no debe poder convertir una firma en un enlace público con fecha. |
| D-61 | 2026-09-12 | **Lista CERRADA de MIME para `deliverable.material`**: PDF, PNG, JPEG, WebP, texto plano, Markdown, CSV y los tres formatos ofimáticos de Office. **Sin ejecutables, sin archivos comprimidos y sin SVG.** | Aceptar cualquier MIME para `material`, que es lo que su nombre sugiere | `data_model` §2.6 delegaba la lista en esta unidad («los MIME que FU-09 fije para material»). Se cierra en vez de abrir por tres motivos: un **ejecutable** en un portal de clientes es una vía de distribución de malware con nuestra marca encima; un **comprimido** es contenido arbitrario que nadie ha validado; y un **SVG es HTML con otro nombre** —lleva `<script>`— y se abriría en el origen del portal, no en el visor aislado que D-45 exige para el HTML. La lista se amplía con una decisión escrita, no con un parche. |

## Decisiones de DU-01 (D-62…D-64)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-62 | 2026-09-12 | **El alta pública de Better Auth deja de existir**: el middleware devuelve **404** a `POST /api/auth/sign-up/**`. La única vía a una cuenta es `/api/acceso/invitacion`, que exige un testigo válido y llama a la librería **por dentro** —una llamada de función, no una petición—, así que el bloqueo no le afecta. | Confiar en que nadie encuentre el endpoint; usar `disableSignUp` de la librería | **Hallazgo de DU-01**: la librería publica ese endpoint por defecto, y con él **cualquiera se daba de alta** en un sitio cuyo acceso es **solo por invitación** (§10-10, RF-59). No era teórico: la primera versión de la prueba creó una cuenta con un `POST` y sin invitación. 404 y no 403 porque un 403 confirma que la ruta está ahí (D-38). `disableSignUp` habría cerrado también la vía interna, que es la que necesitamos. |
| D-63 | 2026-09-12 | **Aceptar una invitación da el correo por verificado.** No se envía un segundo correo de verificación. | Mandar el correo de verificación igual, después del canje | La invitación **se envió a esa dirección**: llegar con su testigo ya prueba que la persona la controla. Pedir después una segunda prueba de lo mismo añade un correo que la gente no encuentra —y que, cuando no llega, deja una cuenta creada que no puede entrar—. El alta fuera de una invitación no existe (D-62), así que no hay otra ruta que pueda aprovechar esto. |
| D-64 | 2026-09-12 | **El enlace de recuperación apunta a `/restablecer`, una pantalla nuestra**, no a la ruta por defecto de la librería. Al aplicar la contraseña nueva **se cierran todas las sesiones**. | Dejar el enlace por defecto y construir la pantalla en esa ruta | El enlace por defecto apunta a `/reset-password`, que en este proyecto **no existe**: el destinatario aterrizaría en un 404 **con el testigo en la barra de direcciones**, que es la peor forma posible de gastar un enlace de un solo uso. Y cerrar todas las sesiones al restablecer es la consecuencia de por qué se restablece: quien lo hace suele sospechar que alguien entró, y dejar vivas las sesiones anteriores deja dentro justamente a esa persona. |

## Decisión de FU-01 (D-65)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-65 | 2026-09-12 | **Los criterios 3 y 4 de FU-01 pasan a ser un freno de CI** (`check:copy`): ningún texto público puede nombrar «Sesión Cero», una agenda o un calendario de terceros, ni llevar un porcentaje, importe, premio, superlativo o caso de cliente **sin declarar su fuente en la misma línea** con `[fuente: …]` o marcarlo `[PENDIENTE: …]`. | Dejarlo en la revisión de la compuerta, que es donde el contrato lo pone | Las dos reglas son precisamente las que se cuelan cuando hay prisa por publicar, y la compuerta de FU-01 se pasa **una sola vez** —«una segunda ronda es cambio de alcance»—: después, cualquier edición de un `.md` entra sin volver a pasar por ella. Un freno las comprueba en cada push y para siempre. **Lo que el freno no puede hacer, y queda escrito**: comprobar si el dato es **cierto**. Comprueba que nadie publique uno sin declarar de dónde sale; la veracidad la firma Ricardo en la compuerta. |

## Decisiones de FU-10 (D-66…D-70)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-66 | 2026-09-12 | **Las cifras de contraste de `style_guide` §2.2, §2.3 y §7.1 sobre `--blue-deep` estaban obsoletas y se corrigen con lo medido**: blanco sobre `--blue-deep` es **11,9:1** (decía 6,5:1) y cyan sobre `--blue-deep` es **5,0:1** (decía 2,8:1), así que **el cyan sí alcanza AA como texto sobre azul oscuro**. Los dos pares pasan a medirse en `check:contraste` (21 mediciones). | Conservar los números del documento y construir los componentes contra ellos | Los números viejos son exactos… **para `#14648C`, el `--blue-deep` anterior**. La tabla del §2.2 ya publicaba `#24394D` y es lo que define `app/tokens.css`, pero las cifras derivadas nunca se recalcularon. Es el mismo patrón que D-59: **el sistema que corre tenía razón y el documento se había quedado atrás**. Consecuencia concreta: la restricción de D-42 —«si la sección oscura necesita cyan legible, la sección es `--indigo`»— **deja de ser una regla de accesibilidad** y queda como preferencia de composición. Y la raíz del problema se cierra midiendo: un número publicado que nadie mide vuelve a envejecer. |
| D-67 | 2026-09-12 | **El orden de C.5 se reordena para poner el formulario de descarga primero**, y los otros ocho siguen el orden literal del anexo. | Seguir el orden literal de C.5, que empieza por la barra de navegación | Lo impone **RF-134**: el formulario es el CTA único de toda página de servicio y «el componente que paga el proyecto», y se valida **antes** que los demás. El propio enunciado de FU-10 pide que esta reordenación se declare aquí, y esto es declararla: no hay conflicto entre las dos reglas, hay una precedencia, y queda escrita. |
| D-68 | 2026-09-12 | **La CSP deja de ser una cabecera estática y pasa a `middleware.ts` con DOS políticas según la superficie**: las páginas prerrenderizadas llevan `script-src 'self' 'unsafe-inline'`; las superficies que se renderizan por petición —`(auth)`, `(hq)`, `(portal)`, `/api`— llevan **nonce nuevo en cada respuesta + `'strict-dynamic'` y SIN `'unsafe-inline'`**. | (a) Dejar `script-src 'self'` como estaba. (b) Poner nonce en todo y volver dinámicas las 26 páginas públicas. (c) `'unsafe-inline'` en todo el sitio. | **Hallazgo grave de FU-10, y no era teórico**: con `script-src 'self'` a secas el navegador **rechazaba los scripts en línea de Next y la hidratación no ocurría** (React error 412). El sitio devolvía 200, las cabeceras parecían impecables y **nada funcionaba**: ni el formulario de descarga, ni el sheet, ni el conmutador de idioma. Ninguna comprobación que no abriera un navegador podía verlo, y por eso lo encontró `test:gesto` y no un `grep`. (a) queda descartada por rota. (b) cambia el renderizado de toda la web pública por una protección que **ahí no protege de nada**: su HTML se genera en la compilación desde `content/`, del repositorio, y no refleja ni un dato externo —y, sobre todo, **un archivo escrito antes de la petición no puede llevar el nonce de esa petición**—. (c) regalaría la protección justo donde sí hace falta. La política estricta cae sobre las superficies que muestran datos de personas, que son las que un XSS querría. `check:runtime` comprueba las dos, que el nonce cambia en cada petición y que es el mismo que Next puso en el HTML. |
| D-69 | 2026-09-12 | **El cierre del sheet es estado de render, no `style` escrito a mano sobre el nodo**, y la velocidad del dedo **caduca a los 100 ms**. | Seguir escribiendo `transition` y `transform` directamente sobre el nodo al soltar, que es lo que hacía | Dos defectos reales, los dos encontrados cuadro a cuadro. (a) El `setState` del propio gesto provoca un render inmediatamente después de soltar, React reescribe las propiedades de `style` que gestiona y **se llevaba por delante la duración recién calculada**: el sheet cerraba **siempre** en los 350 ms por defecto y la cláusula 4 de RNF-45 estaba incumplida sin que se notara leyendo el código. (b) La velocidad se tomaba de la última muestra del puntero **sin mirar cuándo fue**: arrastrar rápido, pararse un segundo y soltar cerraba el sheet **contra un dedo que se había detenido a propósito**. Como efecto del mismo cambio, agarrar a mitad del cierre ahora continúa **desde el valor presentado** —lo que RNF-12 exige y el comentario del componente ya prometía sin que el código lo hiciera—. |
| D-70 | 2026-09-12 | **La revisión «cuadro a cuadro» del gate D3 se convierte en freno ejecutable** (`npm run test:gesto`): un Chromium real, viewport de móvil, y la traslación presentada muestreada en cada `requestAnimationFrame`. Con su prueba negativa —un sheet que incumple las cuatro cláusulas— el proyecto pasa a **trece frenos**. | Dejar la revisión cuadro a cuadro como inspección manual, que es como el criterio 10 la describía | El criterio 10 dice «se verifica cuadro a cuadro, **no por inspección del código**», y una revisión manual se hace una vez: la siguiente persona que toque el componente no la repite. Convertida en freno se repite en cada push. **Su valor no es teórico: encontró los tres defectos de D-68 y D-69 en su primera ejecución.** Coste asumido y declarado: una dependencia de desarrollo (Playwright) y un paso de CI que descarga el navegador. |

## Decisiones de DU-02 (D-71…D-74)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-71 | 2026-09-12 | **Las rutas públicas pasan a ser las canónicas de `ui_wireframes` §1.1**: `/ai` (antes `/slg-ai`), `/holdings` (no existía), `/blog` (no existía), `/doctrina` y `/nosotros`. Se renombran los registros de contenido, se crea el de `SLG_Holdings` y se añade el índice del blog. La portada pasa a `/` y `/en`, y deja de servirse además en `/home` y `/en/home-en`. | Apuntar el menú a los slugs que creó FU-01 y cambiar el documento de diseño | RF-01 exige **cinco destinos**, y tres de ellos no resolvían: `/ai` era `/slg-ai`, y `/holdings` y `/blog` **no existían**. Un marco de navegación con tres enlaces a 404 no es un marco. El documento de diseño es la referencia de rutas de todo lo demás —las salidas del 404, los enlaces del hero, el mapa del sitio—, así que mandan sus rutas y se mueve el contenido, que es lo barato: son marcadores sin copy. El `/home` duplicado se retira por lo de siempre: la misma página en dos URL divide los enlaces y duplica el contenido para los buscadores. |
| D-72 | 2026-09-12 | **El armazón público es un componente de servidor que recibe su ruta como prop**, y no un `layout.tsx` que la lee de una cabecera. | Leer la ruta con `headers()` en `app/(public)/layout.tsx`, que es lo natural en App Router | Leer una cabecera **convierte la página en dinámica**. Las 29 rutas públicas dejarían de prerrenderizarse y cada visita pasaría por el VPS, cambiando el modelo de servicio de toda la web pública por comodidad de implementación. Cada página conoce su ruta en tiempo de compilación; pasarla como prop cuesta una línea por página y conserva el prerrenderizado. |
| D-73 | 2026-09-12 | **Cuando una página no existe en el otro idioma, el conmutador se dibuja DESACTIVADO y dice por qué.** No se esconde y, sobre todo, **no enlaza a la portada**. | Ocultar el conmutador, o mandarlo a la portada del otro idioma | Mandar a la portada es lo que hacen casi todos los sitios bilingües y es exactamente lo que el DoD #2 prohíbe: el visitante pide «esto en inglés», aterriza en otra página, y **pierde dónde estaba** sin que nadie se lo diga. Esconderlo es peor: parece que la función no existe. Desactivado y con motivo es la única opción que no miente. |
| D-74 | 2026-09-12 | **Los frenos pasan a barrer también los archivos NO versionados** (`git ls-files --cached --others --exclude-standard`). | Dejarlos barriendo solo lo indexado, que es lo que hacían | **Hallazgo, y de los caros**: FU-10 se dio por verificada con `check:ci` en verde, y el verde era falso — sus propios archivos todavía no estaban en el índice de git, así que **cinco frenos no los miraron**. Al hacer commit entraron en el barrido y dos se pusieron en rojo: el CI de GitHub habría fallado en el primer push de una unidad declarada terminada. Es la misma familia que R-26 —un barrido que no mira nada siempre pasa—, y se cierra en los cinco frenos a la vez. De rebote destapó dos defectos más: `check:motion` se ponía rojo por los **comentarios** que documentan la regla que vigila, y no reconocía `animationName`, que es la forma que toma un keyframe en un `.tsx`. |

## Decisiones de FU-01 y DU-11 (D-75…D-78)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-75 | 2026-09-13 | **El copy maestro se redacta contra el brief y `knowledge/`, y se marca `copy: temporal` en el frontmatter.** Los 74 registros públicos dejan de tener `[PENDIENTE]`. `check:copy` lista en cada ejecución todo lo que sigue en temporal; pasar a `copy: aprobado` es lo que registra la firma de Ricardo. | Mantener los marcadores `[PENDIENTE]` hasta que llegara el copy de `SLG_Overhauling` | **Decisión de Ricardo el 2026-09-13**, y tiene razón: el material existe —`START_PROJECT.md` §0, §1 y Anexo A, y los nueve archivos de `knowledge/`— y esperar bloqueaba **cinco unidades** (DU-03, DU-04, DU-05, DU-11 y todo M1-A) por un texto que se puede escribir y sustituir. Lo que **no** cambia es la regla dura: cero cifras, premios, casos y nombres de cliente, que `check:copy` sigue vetando sin fuente declarada. Un hueco frena `main`; un borrador declarado, no. **Lo que sigue siendo de Ricardo**: la enumeración literal de las once dimensiones de `SLG_Readiness`, las tres promesas de `CoO as a Service` y las tres líneas de `SLG_Holdings` —el contrato A.3 las pide «tal cual la fuente»—, y el alcance real de `Phoenix PEEx`, `TEAx` y `RETx`, que el brief nombra sin definir. En esos cuatro sitios el texto describe la **forma** del servicio sin inventar sus contenidos. |
| D-76 | 2026-09-13 | **Las rutas públicas se corrigen a las del Anexo A.2**, que estaba ya en el brief: `/ai`, `/holdings`, `/blog`, `/doctrina`, `/nosotros`. (D-71 lo decidió desde `ui_wireframes` §1.1; el Anexo A.2 del brief dice lo mismo, así que las dos fuentes coinciden y no hay conflicto que resolver.) | — | Se registra para dejar constancia de que la decisión tiene **dos** fuentes concordantes y no una. |
| D-77 | 2026-09-13 | **El Markdown del contenido se renderiza con un conversor propio de ~90 líneas**, que construye elementos de React y **nunca HTML arbitrario**. Soporta lo que el contenido usa: encabezados de nivel 2 y 3, párrafos, listas, negrita, cursiva, código en línea y enlaces. | Una librería de Markdown (`marked`, `remark`, `markdown-it`) | Dos motivos, y el segundo pesa más. (a) El presupuesto de JS está al **91 %** del gate D1 y un parser completo no cabe. (b) Una librería de Markdown **acepta HTML embebido** por defecto, y el contenido del blog acabará pasando por manos que no son las nuestras: un `<script>` dentro de un `.md` se ejecutaría. Este conversor lo saca como texto porque no puede hacer otra cosa. Cuando haga falta más sintaxis se amplía aquí y **se ve en la revisión**, que es justo lo que una librería no deja hacer. |
| D-78 | 2026-09-13 | **El repositorio conserva un artículo en borrador permanente** (`content/blog/es/market-fracking.md`, `status: draft`). | Publicar todos los artículos y dar el criterio 2 por verificado | El criterio 2 de DU-11 —«un borrador no se sirve en ninguna parte»— solo se comprueba **si existe un borrador**. Al publicar el único que había, el freno pasó en verde **sin haber ejecutado ni una de las tres comprobaciones del criterio**. Es la misma familia que R-26 y que D-74: un barrido sin casos siempre pasa. El borrador se queda, y su texto explica por qué está ahí para que nadie lo publique por limpieza. |

## Decisiones de DU-03, DU-04 y DU-05 (D-79…D-81)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-79 | 2026-09-13 | **La tabla de rutas públicas es EXPLÍCITA** (`lib/content/rutas.ts`): cada página y cada servicio declara su ruta ES y su par EN, en vez de derivarla del slug del archivo. | Seguir derivando la ruta como `/${slug}`, que es lo que hacía DU-02 | El Anexo A.2 **anida** la oferta: el archivo es `phoenix-peex.md` y la ruta es `/ai/academy/phoenix-peex`. Y `/holdings` lo sirve un registro de **servicio**, no de página. Una regla implícita que necesita cuatro excepciones ya no es una regla: es una tabla mal escrita. La tabla se lee de una vez y se compara con el Anexo A.2 línea a línea; `check:paginas` comprueba que cada ruta responde. |
| D-80 | 2026-09-13 | **`/holdings` lo sirve el registro de `service`, y se retira el registro de `page` que DU-02 había creado.** | Mantener los dos: una página para el destino de menú y un servicio para el contrato A.3 | El Anexo A.2 le asigna el documento **D-11** y el contrato A.3 completo: es una página de servicio que además es destino de menú, no dos cosas. Dos registros para una URL son **dos fuentes para un solo texto**, y la que se edita nunca es la que se sirve. |
| D-81 | 2026-09-13 | **El orden de los bloques vive en el `.md`, no en el componente.** La portada pide sus siete bloques y cada servicio sus seis **por posición** a `secciones()`, que parte el cuerpo por sus encabezados de nivel 2. | Componer el orden en el `.tsx`, leyendo cada bloque por su nombre | RF-27 promete que **añadir un servicio es añadir un archivo**, y eso solo es cierto si la estructura viaja con el contenido. Pedirlos por **posición** y no por título permite además traducir los encabezados —«Qué es» / «What it is»— sin que el orden dependa del idioma. El riesgo que esto abre —que alguien reordene o borre un bloque al editar un `.md`— es exactamente lo que comprueba `check:paginas` en las 22 páginas de servicio y en las dos portadas, en cada push. |

## Decisiones de DU-06, DU-07, FU-11 y DU-08 (D-82…D-86)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-82 | 2026-09-13 | **La lista de dominios de correo gratuito vive en una TABLA** (`free_email_domain`), no en un archivo ni en una variable de entorno. Cierra el `[PENDIENTE]` que `data_model` §5 y `api_contracts` §11.8 dejaban abierto para FU-11, y **retira `FREE_EMAIL_DOMAINS_SOURCE`**. | Un archivo en `content/`; una variable de entorno con la lista separada por comas | RF-32 exige «dato editable **sin desplegar**». Un archivo del repositorio no lo es: cambiarlo obliga a recompilar y volver a desplegar, que es literalmente lo que el requisito prohíbe. Una variable de entorno tampoco: cambiarla reinicia el servicio. Una fila se añade con un `INSERT` y surte efecto en la petición siguiente — comprobado en `test:descargas`, que inserta un dominio y verifica el rechazo **sin reconstruir nada**. La caché en proceso dura un minuto: «sin desplegar» no quiere decir «sin esperar un minuto». |
| D-83 | 2026-09-13 | **El contador del límite de peticiones vive en la base de datos**, con la clave **hasheada**. | Un contador en memoria del proceso, que es lo barato | Con dos instancias del servidor un contador en memoria permite **el doble** de peticiones, y con tres el triple: el límite deja de existir sin que nadie se entere. Y la clave va hasheada porque una IP es un dato personal y una dirección de correo lo es más: la tabla necesita **contar**, no saber a quién. |
| D-84 | 2026-09-13 | **El estado por defecto del reveal al scroll es VISIBLE.** Se esconde, desde el componente y solo al montar, lo que está **por debajo de la ventana**; y hay un rescate a los 3 s por si el observador no dispara. | Dejarlo como estaba: `opacity: 0` en CSS y visible al cruzar | **Hallazgo de una captura de pantalla, no de un test**: escrito al revés, la portada entera salía **en blanco por debajo del hero**. Y no es un caso raro: pasa sin JavaScript, con el script bloqueado, si la hidratación falla, al imprimir, y en cualquier herramienta que no haga scroll. Un contenido que se esconde por defecto y depende de que un observador dispare **apuesta la página entera** a que nada falle. Escondiendo solo lo que está fuera de pantalla no hay parpadeo —no se ve desaparecer lo que no estaba a la vista— y el fallo posible pasa de «no se ve nada» a «no hay animación». |
| D-85 | 2026-09-13 | **La 404, la 500 y sus textos se importan del JSON de `content/ui` directamente**, no a través del cargador de contenido. | Usar `loadUiStrings()` como el resto del sitio | `error.tsx` **tiene que ser** un componente de cliente —un límite de error solo puede serlo— y el cargador usa `node:fs`, que no existe en el navegador. Arrastrarlo hasta ahí **rompía la compilación entera** con un error de Turbopack que no nombraba la causa. El texto sigue viviendo en `content/ui` y sigue cumpliendo RF-16: lo único que cambia es cómo llega. |
| D-86 | 2026-09-13 | **El `canonical` se compara contra el origen que la propia página declara**, no contra el del servidor de pruebas; y **los títulos se comparan dentro de cada idioma**, no entre idiomas. | Comparar todo contra la base del servidor y exigir títulos distintos en las 78 rutas | Las páginas se **prerrenderizan**: el `canonical` se fija en la compilación y compararlo con el puerto del servidor de prueba daba un rojo permanente y falso. Y exigir títulos distintos entre idiomas obligaría a **inventar una traducción de un nombre propio** —`/ai` y `/en/ai` se llaman los dos `SLG_AI`—, que es justo lo que RF-14 prohíbe. El par lo declara `hreflang`, que es para lo que existe. |

## Decisiones de DU-09 y DU-10 (D-87…D-90)

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-87 | 2026-09-13 | **El barrido de la cola de CRM corre cada 20 s**, en lotes de 10, con reserva de 5 min. Cierra el `[PENDIENTE]` que `architecture` §6.2 dejaba abierto («la restricción dura es < 60 s»). | 50 s, que también cumple; o un intervalo igual al escalón | **Tiene que ser menor que el escalón de espera más corto** —1 minuto, RF-50— o la espera real no sería la especificada y el gate D7 mediría otra cosa. 20 s deja margen de sobra para que una vuelta larga no se solape con la siguiente, y el volumen de la v1 son unas pocas capturas al día: no compite con el tráfico. El lote de 10 es lo que impide que una caída larga del CRM vuelva como avalancha en el minuto en que se recupera. |
| D-88 | 2026-09-13 | **`lead_capture` gana una columna `message`** para el texto libre de `/contacto`. | Meterlo en el `jsonb` de `utm`; no guardarlo y mandarlo solo al CRM | La «ausencia deliberada» de `data_model` §5.11 prohíbe el **pipeline** —etapa, propietario, importe, probabilidad, próximo paso—, que vive en el CRM. El mensaje no es pipeline: es lo que la persona escribió, y **sin él un formulario de contacto no sirve para nada**. Meterlo en `utm` convertiría un mapa de procedencia en un cajón y estorbaría a la primera consulta por campaña. Viaja al CRM dentro de la nota y aquí queda como respaldo. |
| D-89 | 2026-09-13 | **Una sola máquina de captura, tres puertas**: descarga, contacto y solicitud de Doctrina llaman a `registrarCaptura`, y lo único que cambia es el `source` y si hay archivo que firmar. | Un manejador por formulario, que es lo natural al escribirlos uno detrás de otro | Tres caminos paralelos son **tres sitios donde olvidarse del campo trampa** — y el que se olvida no da ningún error: deja pasar. Lo mismo con el límite, el rechazo de dominios gratuitos y el `consent_at`. Una máquina y tres entradas hace que añadir un formulario nuevo herede la protección en vez de tener que acordarse de ella. |
| D-90 | 2026-09-13 | **`check:lighthouse` repite UNA vez cuando la medición falla**, y distingue «no se pudo medir» de «suspende». | Dejar que un `runtimeError` o un 0 cuenten como suspenso | Un rendimiento de **0** no significa que la página vaya lenta: significa que el audit no llegó a correr. Pasó en una ejecución real —96 en la anterior y en la siguiente— y habría puesto el CI en rojo por algo que no es del sitio. Es la misma familia que el timeout de `check:dns` (D-…): **un freno que se pone rojo por sí mismo enseña a ignorar el freno**, y entonces deja de servir para lo que se construyó. |

## Acciones de seguridad previas a la ejecución

| # | Acción | Dónde se sigue |
|---|--------|----------------|
| S-01 | Existe una **acción de seguridad previa a la ejecución**, ya identificada y asignada a Ricardo. **Nada relacionado con ella se escribió en ningún archivo del repositorio** — verificado. | **Diferida por decisión de Ricardo (2026-09-08)**: se ejecuta al final del proyecto, no ahora. **Condición innegociable que hace aceptable el aplazamiento**: la credencial afectada es de **construcción**, no de producción; las credenciales de producción se crean nuevas cuando se necesiten y **nunca** se reutiliza la afectada en ningún entorno desplegado. La rotación es requisito de go-live: sin ella, el DoD no se da por cumplido. **Fuera de este repositorio.** El detalle, el estado y el seguimiento operativo se llevan por el canal privado: este repositorio es **público** (§10-6) y un registro público de una acción de seguridad es, por sí mismo, información útil para un atacante. |

## Pendientes de decisión

Las elecciones de producto de **correo transaccional** (D-22) y **destino de backups** (D-21) están
cerradas, **P-5 incluida** (D-43). Quedan solo dos sub-decisiones de configuración, que se fijan en M0:

| # | Pendiente | Tipo | Estado |
|---|-----------|------|--------|
| ~~P-5~~ | **Monitorización externa de disponibilidad, fuera del VPS.** La abrían `risks.md` R-29 y RF-130 (gate D11). | Elección de **categoría** | ✅ **Cerrada por D-43**: servicio de uptime dedicado con tramo gratuito, externo al VPS. Queda elegir el producto concreto con 2–3 candidatos (paso 5), lo que **no bloquea el arranque**: afecta a FU-05 y DU-25, ambas fuera de M0-A. n8n queda como monitor **secundario**. |
| P-3 | Dirección remitente visible, tras D-24: `From` en el subdominio con `Reply-To` a `support@` (recomendado), o `From` en la raíz apoyándose solo en alineación DKIM | Sub-decisión de configuración | Abierta, se fija en M0 (antes de FU-08) |
| P-4 | Nombre exacto del subdominio de envío | Sub-decisión de configuración | Abierta, se fija en M0 (antes de FU-08) |

**Ninguna de las tres bloquea la aprobación del plan.** P-3 y P-4 se fijan en M0 antes de FU-08.
El producto de monitorización se elige antes de FU-05, con el mismo procedimiento que P-1 y P-2:
2–3 candidatos con contrapartidas, y elige Ricardo.

Elecciones de producto resueltas con Ricardo el 2026-09-08 (AGENTS.md Regla 7: elige quien vive las consecuencias):

| # | Categoría decidida | Falta elegir | Ref. |
|---|--------------------|--------------|------|
| ~~P-1~~ | ~~Servicio transaccional dedicado (D-15)~~ | ✅ **Resuelta: Resend (D-22)** | — |
| ~~P-2~~ | ~~Object storage externo (D-20)~~ | ✅ **Resuelta: Cloudflare R2 (D-21)** | — |
