---
type: docs
title: Decision log
project: slg_website
status: planning
timestamp: 2026-09-08
---

# Decision log — slg_website

Decisiones significativas. Las 13 decisiones HITL previas de Ricardo viven en `START_PROJECT.md` §10
y **no se re-exploran**; aquí se registran desde D-14 las que toma la planificación.

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-14 | 2026-09-08 | **Perfil de activo: `software-app`** | `intelligence-product` (compuesto) | El activo es una aplicación desplegable con superficie de administración. Los gates extra (marketing, identidad, API) se declaran en el brief (Anexo D), no exigen perfil compuesto. Regla del perfil sobre superficie de admin/operaciones: aplica (HQ lo es). |
| D-15 | 2026-09-08 | **Correo transaccional: servicio transaccional dedicado** con dominio verificado (SPF/DKIM/DMARC). Producto concreto pendiente de elegir en el paso 5 con 2–3 candidatos. | SMTP de Google Workspace tras la migración | Entregabilidad medible, webhooks de rebote y logs, que un buzón corporativo no da. **Corrección posterior (D-23)**: la alternativa de Google Workspace queda además sin objeto, porque el correo se queda en Microsoft 365 y no hay migración. La categoría se sostiene igual: enviar correo automatizado desde un buzón de persona produce entregabilidad mala, topes de envío y ninguna prueba de entrega. |
| D-16 | 2026-09-08 | **Anti-abuso sin desafío de terceros en v1**: límite de peticiones + honeypot + lista de dominios de correo gratuito, todo propio. | Desafío anti-bot de terceros | Cero scripts de terceros en la capa pública: protege el gate D1 (Lighthouse ≥ 90, JS inicial < 150 KB gz) y la promesa privacy-first. Reversible: se añade si aparece spam real. |
| D-17 | 2026-09-08 | **11 documentos de descarga** (D-01…D-11), uno por página de servicio, incluidos D-04 (Customize Programs) y D-05 (AI Coaching for Directors). Resuelve la contradicción entre §10-12 ("9") y A.2/A.4 ("11"). | 9 documentos; 11 registros lanzando con los que existan | El contrato de página A.3 §5 exige la descarga como **CTA único** de cada página de servicio. Con 9, dos páginas quedarían sin CTA y necesitarían un contrato alternativo. Los PDFs se producen en SLG_Overhauling; mientras falten, la página muestra "disponible próximamente" y captura el email igual (mecanismo ya previsto en A.4). |
| D-18 | 2026-09-08 | **MCP del CRM registrado con `--scope local`** (`~/.claude.json`), nunca `--scope project`. | `--scope project` (`.mcp.json` en el repo); `--scope user` | El repo `slg_website` es **público** (§10-6) y `.gitignore` no cubre `.mcp.json`: el scope de proyecto habría escrito credenciales dentro del repositorio. El scope local mantiene el secreto fuera del control de versiones. Verificado: `git status` limpio tras el alta. |
| D-19 | 2026-09-08 | **Entrega de capturas al CRM: adaptador de dos modos**, seleccionable por variable de entorno. Modo `contact_note` (lo que el CRM permite hoy: `POST /contacts` + `POST /notes`) y modo `lead_admission` (`POST /api/v1/leads` con alcance `leads:write`, cuando exista). | Solo contacto+nota en v1; ejecutar el `/iterate` del CRM antes de M2 | M2 no queda bloqueado por un cambio en otro repositorio (`~/Dev/crm_slg`). El spec-delta del CRM se enchufa después sin reabrir el DU de captura ni migrar datos. Coste: el `api_contracts` debe especificar los dos modos y el DU de captura probar ambos. |
| D-20 | 2026-09-08 | **Backups a object storage S3-compatible externo, en proveedor distinto de Hostinger, con tramo gratuito.** Producto concreto pendiente de elegir en el paso 5 con 2–3 candidatos. | Google Drive de SLG; segundo VPS o NAS propio | El riesgo "un solo VPS" (§9) no se mitiga si el backup vive en la máquina que puede morir. El tramo gratuito mantiene el coste en cero mientras el volumen v1 sea pequeño. El DoD #8 exige restauración probada, que es más fiable sobre API S3 que sobre Drive. |
| D-21 | 2026-09-08 | **Destino de backups: Cloudflare R2.** Resuelve P-2 dentro de la categoría fijada en D-20 (object storage S3-compatible externo, proveedor distinto de Hostinger). | Backblaze B2 (recomendación del análisis, por Object Lock); IDrive e2 | Elección de Ricardo (AGENTS.md Regla 7). R2 da **egreso $0 a cualquier volumen, sin franquicia**: restaurar nunca genera factura, que es justo cuando hay una emergencia. Tramo gratuito 10 GB-mes + 1M operaciones clase A + 10M clase B, recurrente. **Contrapartidas asumidas**: (a) sin Object Lock por API estándar, la protección contra borrado malicioso hay que resolverla de otro modo — ver R-nuevo en `planning/risks.md`; (b) las operaciones son la partida medida: el cliente de backup debe configurarse para no dispararlas. El script de backup y restauración se escribe contra API S3 genérica: cambiar de proveedor es endpoint + credenciales en variables de entorno. |
| D-22 | 2026-09-08 | **Correo transaccional: Resend.** Resuelve P-1 dentro de la categoría fijada en D-15. | SMTP2GO (suplente inmediato); Zoho ZeptoMail; descartados con motivo: Brevo (estampa su logo en el plan gratuito), Mailgun (gratis solo a 5 destinatarios autorizados), Amazon SES (sin tramo gratuito para clientes nuevos), Postmark y MailerSend (tramos demasiado cortos) | Elección de Ricardo (AGENTS.md Regla 7). 3.000 correos/mes gratis frente a los 1.000 del suplente, retención de registros de 30 días frente a 5, sin marca de terceros en los correos, y SMTP estándar disponible como vía de escape. **Condición de diseño**: el DU de correo se construye como adaptador tras variable de entorno y habla SMTP, no el SDK propietario — así cambiar de proveedor cuesta tres variables de entorno y ninguna línea de código. **Aviso privacy-first**: el seguimiento de aperturas y clics debe quedar DESACTIVADO por dominio. |
| D-23 | 2026-09-08 | **El correo corporativo de SLG permanece en Microsoft 365.** No hay migración a Google Workspace. Los MX de Outlook se mantienen. | Migrar a Google Workspace (supuesto del brief §7 y §1 Constraints, ahora descartado) | Decisión de Ricardo. **Efectos sobre el plan**: (a) desaparece la urgencia de F.2-3 — no se va a cancelar M365, así que el registro en Entra ya no compite contra una fecha de cancelación, y **puede reutilizarse el tenant existente**; (b) el registro SPF de la raíz debe llevar el `include` de Resend **junto al de Outlook**, sin romperlo — es la operación delicada que sustituye al riesgo de la migración; (c) el MX de Outlook entra formalmente en la lista de "no tocar" del §7, junto a `crm`, `n8n`, `evolution` y `academy`. |
| D-24 | 2026-09-08 | **Subdominio de envío dedicado para el correo transaccional.** Resend verifica un subdominio propio (p. ej. `mail.softlandingglobal.com`); el registro SPF de la raíz **no se toca** y sigue autorizando solo a Outlook. Resuelve el `[PENDIENTE]` abierto en R-38. | Convivencia de dos `include` en el SPF de la raíz (opción descartada) | Elección de Ricardo. Aísla por completo la reputación de envío automatizado de la del correo humano: un problema de entregabilidad en uno **no puede** arrastrar al otro, y publicar los registros del subdominio no puede romper el correo corporativo, porque son entradas DNS distintas. **Contrapartida asumida**: la dirección remitente deja de ser exactamente la que fija el brief (`support@softlandingglobal.com`) — ver la sub-decisión pendiente sobre el `From`. `[PENDIENTE: nombre exacto del subdominio]`, se fija en M0. |

## Decisiones tomadas por los `design_docs` (D-25…D-42)

Las **24 declaraciones** que los cinco `design_docs` hicieron por su cuenta —porque el brief no las
fijaba—, agrupadas en **18 entradas** tal como las consolida `design_docs/design_summary.md` §1.
Origen: `data_model` §11.2 (5), `api_contracts` §13.3 (12), `architecture` §15.3 (7) y la regla
derivada de `style_guide` §2.3. Nada aquí es nuevo: se transcribe lo ya escrito.

> **Marca de atención.** Las filas marcadas **[IRREVERSIBLE-TRAS-FU-04]** son las que dejan de ser
> gratuitas en cuanto corre la primera migración (FU-04, unidad de M0-A): revertirlas después es
> migración de datos, no una edición. Son las que Ricardo debe mirar con más atención antes de
> aprobar el bloque.

| # | Fecha | Decisión | Alternativas consideradas | Razón |
|---|-------|----------|---------------------------|-------|
| D-25 | 2026-09-08 | **Tamaño máximo de subida por destino** (`data_model` §2.6): **25 MB** en `downloads`, **50 MB** en `deliverables` (`pdf`/`material`), **5 MB** en `html`, **1 MB** en `md`, **tope duro 50 MB**. | Otros topes: RNF-25 dejó el número abierto y mandó fijarlo aquí | Cierra el `[PENDIENTE]` de RNF-25. Impacto de revertir: bajo si sube, **alto si baja** — FU-04 y FU-09 usan la constante única, y el presupuesto del tramo gratuito de R2 (10 GB-mes) está calculado sobre estos topes. |
| D-26 | 2026-09-08 | **Enumerados de estado deliberadamente mínimos** (`data_model` §3.3): `organization.status` con **2** valores (`active`, `archived`) y `project.status` con **3** (`active`, `completed`, `archived`). **[IRREVERSIBLE-TRAS-FU-04]** | Añadir estados comerciales (`paused`, `negotiation`) | B.2, RF-77 y RF-79 piden «estado» sin enumerarlo. Los estados comerciales mueven la frontera (a) de `scope.md`: eso es el CRM, no la web. **Cambiar el enumerado después de FU-04 es migración de datos.** |
| D-27 | 2026-09-08 | **Convenciones de esquema en la frontera con Better Auth** (`data_model` §1.2, §2.1, §2.3): **PK `text` uniforme** en las 19 tablas, **enumerados como `text` + `CHECK` nombrado**, renombres **`member`→`membership`** y **`apikey`→`api_key`**, alcances en columna propia `scopes`, `timestamptz` en todas las fechas. *(Agrupa 3 de las 5 declaraciones de §11.2.)* **[IRREVERSIBLE-TRAS-FU-04]** | Tipo `ENUM` nativo de Postgres; conservar los nombres de Better Auth (`member`, `apikey`) | El brief no entra en el tipo de las claves ni en cómo se escriben los enumerados. **Muy alto después de la primera migración: es el tipo de la clave primaria de las 19 tablas. Prácticamente irreversible una vez corre FU-04.** |
| D-28 | 2026-09-08 | **Caducidad de las URLs firmadas** (`api_contracts` §11.9): **15 min** descarga pública · **10 min** entregable del portal · **30 min** subida por API. Tres **variables de entorno**. | Tres constantes en código en vez de tres variables de entorno | Cierra el `[PENDIENTE]` de RNF-20. Impacto de revertir: bajo, son variables de entorno — pero alargarlas debilita el gate D10 y acortarlas rompe la descarga desde móvil con mala cobertura (DoD #1). |
| D-29 | 2026-09-08 | **Forma transversal de `/api/v1`** (`api_contracts` §2.1, §2.5, §2.7, §3.9): **sobre de error único** (`code` estable, `message` sin internos, `request_id` = `audit_log.id`), **paginación por cursor opaco** (50 por defecto, 200 máximo), **sin `PATCH`, `PUT` ni `DELETE`**, y `openapi.json` **generado** de los mismos esquemas que validan. *(Agrupa 4 de las 12 de §13.3.)* | — B.5 pide una API v1 con alcances; no fija forma de error, paginación, verbos ni cómo se produce la especificación | **Alto una vez publicada**: cambiar la forma del error o la paginación rompe a todo consumidor. Antes de M5 el coste es cero. |
| D-30 | 2026-09-08 | **En la cola del CRM, las cinco esperas preceden a los cinco intentos** (1 min → 10 min → 1 h → 6 h → 24 h) (`api_contracts` §8.1). **Resuelve C-1.** | Primer intento inmediato — obliga a descartar la espera de 24 h o a subir el tope a 6 intentos, lo que toca la restricción `crm_delivery_attempt_bounded` | B.6-3 y RF-50 enumeran cinco esperas; el esquema acota a cinco intentos. Solo encajan si cada espera precede a un intento. **Coste asumido**: el contacto llega al CRM ~1 min después de la captura. |
| D-31 | 2026-09-08 | **Visibilidad por fila de las claves de API** (`api_contracts` §3.6): una clave **acotada a una empresa** ve solo entregables `client` **publicados**; una **clave de SLG** lo ve todo. | — B.5 fija alcances por verbo, no visibilidad por fila | **Alto**: es la regla que sostiene el aislamiento en la superficie de agentes (DoD #5, gate D9). Relajarla filtra borradores de una empresa a otra. |
| D-32 | 2026-09-08 | **Contrato criptográfico y de garantía de los webhooks** (`api_contracts` §9.3, §9.4 + `architecture` A-07): cabecera **`X-SLG-Signature`**, cadena firmada **`<timestamp>.<cuerpo crudo>`**, tolerancia de **5 min**, **dedupe por `X-SLG-Delivery`**, entrega **«al menos una vez»**. *(Agrupa las dos: son la misma decisión.)* | — B.7 exige «firma HMAC-SHA256 en cabecera» y no fija nombre, cadena, tolerancia ni garantía de entrega | **Alto tras el primer suscriptor**: cambiar la cadena firmada invalida toda verificación existente. Antes, cero. |
| D-33 | 2026-09-08 | **Los webhooks no transportan datos personales** (`api_contracts` §10.1, §10.3, §10.4, §10.6, §10.8): ni correo, ni nombre, ni token, ni cuerpo de aviso, ni URL firmada — solo identificadores y el hecho. | Transportar en la carga útil correo, nombre, token, cuerpo de aviso o URL firmada | El brief lista los nueve eventos, no su carga útil. Bajo técnicamente, **alto en promesa**: es privacy-first aplicado. Revertirlo saca datos personales a un canal saliente. |
| D-34 | 2026-09-08 | **`publish` es obligatorio y sin valor por defecto** en `POST /announcements` (`api_contracts` §3.7). | `publish` con valor por defecto implícito | B.5 no dice si publicar es el defecto. Un defecto implícito publica avisos a clientes por omisión: el fallo es **silencioso y visible para el cliente**. |
| D-35 | 2026-09-08 | **Plantillas exactas de la nota del CRM**, una por `source`, con las siete reglas de composición (`api_contracts` §6.3). | — B.6-2 da un ejemplo de nota, no un contrato | Bajo: cambiar la plantilla no migra nada. Pero las notas ya escritas en el CRM quedan con el formato viejo. |
| D-36 | 2026-09-08 | **El adaptador de correo habla SMTP estándar** (`api_contracts` §11.3): **no existe variable de SDK propietario**, y esa ausencia es la decisión. Materializa la condición de diseño que **D-22** ya impone. *(`design_summary` §1 propone anotarlo bajo D-22 en vez de darle ID propio; se registra como fila con el puntero.)* | Variable de configuración de un SDK propietario del proveedor | D-22 lo exige como condición; ningún documento lo había materializado en variables. Bajo hoy, **alto mañana**: es lo que hace que cambiar de proveedor cueste tres variables y ninguna línea de código. |
| D-37 | 2026-09-08 | **Ejecución de colas dentro del servicio `slg-web`** (`architecture` §6.2, §6.3): el ejecutor **no** vive en un servicio aparte ni en un orquestador externo (A-01), y reclama trabajo por **reserva con plazo empujando `next_attempt_at`**, sin estado «en curso» nuevo (A-02). *(Agrupa A-01 y A-02.)* | Servicio trabajador aparte u orquestador externo; estado «en curso» nuevo en el esquema | El brief no dice dónde corre el trabajo en segundo plano; RF-115 solo exige que n8n sea opcional. Medio: separar el trabajador es la salida de escape ya documentada (§6.7) y no reabre el esquema; cambiar el mecanismo de reserva sí toca las tres colas a la vez. |
| D-38 | 2026-09-08 | **404, nunca 403**, al cruzar de superficie o de empresa (`architecture` A-03 + `ui_wireframes` §1.4, §8.1). *(Agrupa: los dos documentos dicen lo mismo.)* | Devolver 403 — RF-71 dice literalmente «404/403» y deja la ambigüedad abierta | Bajo de construir, **alto de seguridad**: un 403 confirma que la ruta o el recurso existen. Coherente con los mensajes neutros de RF-59. |
| D-39 | 2026-09-08 | **En `/api/v1`, el límite de peticiones (429) se comprueba antes que el alcance (403)** (`architecture` A-04, §2.5). | Comprobar el alcance (403) antes que el límite (429) | El brief fija ambos controles, no su orden. Bajo: es una línea de middleware. Invertirlo deja que una clave martillee endpoints prohibidos sin consumir cuota. |
| D-40 | 2026-09-08 | **La caché de métricas del CRM vive en memoria del proceso**, no en una tabla (`architecture` A-05, §3.6). | Caché persistida en tabla | RF-55 exige caché de 5 minutos y no dice dónde. Bajo: un reinicio cuesta una lectura. Llevarla a tabla obliga a declararla en `data_model`. |
| D-41 | 2026-09-08 | **El espejo de la tabla `download` se sincroniza en el despliegue y nunca borra filas** (`architecture` A-06, §5.3). | Borrar filas del espejo al retirar un documento | B.4 define el puente contenido↔base de datos sin decir qué pasa al retirar un documento. **Alto**: borrar filas rompe la trazabilidad de las capturas anteriores, que apuntan a `download` como ancla de identidad. |
| D-42 | 2026-09-08 | **Regla derivada de los contrastes medidos** (`style_guide` §2.3): si una sección oscura necesita cyan legible, la sección es **`--indigo`, no `--blue-deep`** (2,8:1 vs 5,3:1); y **`--line` divide pero no delimita** un control (1,6:1). *(`design_summary` §1 la marca `[propuesta]`: el documento no la declara como decisión a registrar.)* | Mantener `--blue-deep` con cyan encima; usar `--line` como delimitador de control — el Anexo C admite el cyan como acento en secciones oscuras sin medir el contraste sobre cada fondo | Los números están medidos sobre los hexadecimales del propio kit. Bajo de construir, pero revertirlo es publicar texto que **falla AA**: choca de frente con el gate D2 y RNF-04. |
| D-43 | 2026-09-08 | **Monitorización externa: servicio de uptime dedicado con tramo gratuito, ejecutado fuera del VPS.** Cierra P-5, RF-130 y el gate D11; resuelve R-29. Producto concreto pendiente de elegir con 2–3 candidatos (paso 5). | n8n propio (lo sugiere §9 del brief); Cloudflare, ya presente por D-21; aplazar a v1.1 relajando D11 | Elección de Ricardo. n8n corre en el **mismo VPS** que debería vigilar (`167.88.42.76`): si el servidor cae, el monitor cae con él y nadie se entera — sirve como señal **secundaria**, nunca como principal. Concentrar backups y monitorización en un mismo proveedor reduciría la independencia que motivó D-20. |
| D-44 | 2026-09-08 | **Anillo de foco de dos capas**: capa exterior `--cyan` (#50B4DC), capa interior `--blue-primary` (#2878B4) o `--ink`. Resuelve el conflicto CF-3. | Anillo sólido en `--blue-primary`; mantener `--cyan` sólido como lista el Anexo C.1 | Elección de Ricardo. El Anexo C.1 lista `--cyan` entre los usos de anillo de foco, pero medido da **2,4:1 sobre `--paper`**: no cumple RNF-05 ni el gate D2, y el DoD #7 exige Lighthouse Accessibility ≥ 90. Las dos capas **conservan la intención cromática del kit** y añaden el contraste que le faltaba: es una precisión del kit, no una contradicción. Se fija el token en FU-02 y se verifica en FU-10. |
| D-45 | 2026-09-08 | **El visor de entregables HTML se sirve desde un ORIGEN SEPARADO** (subdominio propio), como norma. Resuelve el conflicto CF-4 en favor de lo que `data_model` §3.10 y `ui_wireframes` §7.3 ya prometían. El `iframe sandbox` sin `allow-same-origin` y la CSP estricta **se mantienen**: son defensa en profundidad, no la alternativa. | Solo `iframe sandbox` + CSP, como especificaba `architecture` §11.3 | Elección de Ricardo. Parte de los entregables HTML los generan **agentes Hermes**: con origen separado, un script hostil dentro del entregable no puede leer cookies de sesión ni datos de la aplicación, porque el navegador lo aísla por política de origen. Con solo sandbox, la protección depende de que la configuración sea correcta **siempre**; un fallo de configuración expone la sesión del cliente. Se construye en DU-19. `[PENDIENTE: nombre del subdominio del visor, se fija en M4]`. |
| D-46 | 2026-09-08 | **Hermes Agent asume, por cron, las compuertas de revisión que no exigen a Ricardo.** Mitigación de R-16 (aprobador único). Alcance: ejecutar `/review` por milestone con contexto limpio, correr las pruebas E2E del DoD en contenedor y devolver veredicto estructurado, y recordar las compuertas pendientes con su paquete de decisión ya preparado. **Fuera de su alcance, por gobernanza: el Planning Gate (Regla 1), la compuerta de copy FU-01 y las elecciones de producto (Regla 7)** — esas siguen siendo de Ricardo. | Mantener a Ricardo como único aprobador de todo; construir un validador dentro de slg_website | Propuesta de Ricardo. R-16 tenía probabilidad **Alta** e impacto **Alto**: el proyecto se detiene esperando decisiones de una sola persona. `AGENTS.md` solo exige que la revisión por milestone la haga *"un revisor independiente con contexto limpio"* — no dice que sea Ricardo, así que delegarla **no viola el método**. **Sin cambio de alcance en este repositorio**: el cron vive en Hermes Agent y consume lo que este proyecto ya produce — el pipeline de CI de FU-05 y, tras M5, `POST /api/v1/events` con `agent_event`. §5.3 mantiene «Hermes como validador» en *previsto, no se construye*: esta decisión **no lo mueve a alcance**, solo confirma quién opera la compuerta desde fuera. |
| D-47 | 2026-09-08 | **`--blue-deep` pasa de `#14648C` a `#24394D`** (azul sobrio oscuro). El color de enlace se separa en su propio token `--slg-link`, que conserva `#2878B4`. | Mantener `#14648C` del kit · `#1E3A5C` acero · `#282878` índigo | Elección de Ricardo tras verlo renderizado. Medido, `#14648C` daba **matiz 200° y saturación 75%**: frontera teal, y en pantalla se leía verdoso. `#24394D` da **209° y 36%** — azul sin discusión, y la saturación baja es lo que produce la sensación metálica. El contraste sube de 6,5:1 a **11,9:1**. **Efecto secundario gestionado**: el kit usaba `--blue-deep` también para enlaces, y un enlace en ese azul apenas se distingue del cuerpo. Corrige el Anexo C.1 del brief. |
| D-48 | 2026-09-08 | **Entidad `contact` ligera.** `organization` **no** lleva `primary_contact` como columna; en su lugar una tabla `contact` con `organization_id`, nombre, correo, cargo e `is_primary`, independiente de si esa persona tiene cuenta de portal. Resuelve H-04. | `primary_contact_user_id` con FK a `user` · texto libre en `organization` · exigir que el contacto principal sea siempre usuario invitado | Elección de Ricardo. Su definición —«un contacto vinculado a una empresa con etiqueta de tipo principal»— describe a una persona que **puede no tener cuenta**, y eso no cabía ni en `user` ni en `membership`. **Frontera que se defiende**: esta tabla responde a «¿a quién llamo en esta empresa cliente?», y **no** lleva estado comercial ni pipeline — eso vive en el CRM (§10-13). Una columna de etapa, importe o probabilidad aquí rechaza la revisión. |
| D-49 | 2026-09-08 | **Monitorización externa: UptimeRobot**, plan gratuito. Cierra EXT-7 y P-5; con D-43 completa RF-130 y el gate D11. | Better Stack (10 monitores, 3 min, captura al fallar) · Hyperping (descartado: su tramo gratuito solo era verificable en su propio blog comparativo) · Cloudflare Health Checks (descartado: concentraría backups y monitorización en la misma cuenta, reduciendo la independencia que motivó D-20) | Elección de Ricardo. 50 monitores y 5 min sobran para los 4–6 que necesitamos. Sin tarjeta de crédito. **Lo decisivo**: integra con **n8n** en el plan gratuito, así que el aviso principal viaja fuera del VPS y n8n queda como señal **secundaria** — el reparto que fija D-43. **CORREGIDO EN LA PRÁCTICA**: Ricardo comprobó al dar de alta que **la caducidad de SSL y de dominio NO viene en el plan gratuito**, pese a que su página de precios la lista ahí. Vale la comprobación empírica, no la página. Consecuencia registrada en **R-39**: la renovación del certificado queda sin vigilancia automática y hay que cubrirla antes del go-live. **SEGUNDA CORRECCIÓN EN LA PRÁCTICA (2026-09-10, FU-05 criterio 8)**: el propio elemento que se llamó «lo decisivo» —la integración con n8n vía webhook— **tampoco está en el plan gratuito**: la sección Integrations de UptimeRobot muestra Webhook, Slack, Telegram y Mattermost como «Upgrade to access». Verificado en el panel real, no en la página de precios, igual que la corrección anterior. **No rompe D-43**: el requisito real era un canal que no dependa del VPS, y el correo (enviado por los servidores de UptimeRobot, no por el VPS) ya lo cumple — hoy es el único canal activo. n8n como señal secundaria queda sin materializar hasta que se pague el plan o se busque una alternativa; no bloquea el cierre de FU-05. |
| D-50 | 2026-09-09 | **El gate D1 se mide por Lighthouse en CI, no por presupuesto de KB.** Umbral: Lighthouse móvil ≥ 90 en Performance, Accessibility, Best Practices y SEO, y LCP < 2,5 s (RNF-01, RNF-02), medido con `scripts/ci/check-lighthouse.ts` contra el build real arrancado con `next start`. **RNF-03 (< 150 KB gz) queda retirada.** Resuelve R-40. | **(b)** subir el presupuesto a 180 KB como detector de regresiones, manteniéndolo junto a Lighthouse — recomendación de la sesión anterior, no elegida · **(c)** mantener 150 KB, lo que exige abandonar App Router y contradice §10 | Elección de Ricardo, entre las tres salidas que registraba R-40. El suelo de React 19 + Next 16 App Router es 172 KB comprimidos en una página vacía, sin una sola librería propia — por encima de los 150 KB del gate — pese a que Lighthouse ya daba 98/100/92/100 con LCP 2,4 s: los 150 KB eran un proxy mal calibrado del objetivo, no el objetivo. Lighthouse mide la experiencia real y es más difícil de engañar con un número de bytes. Verificado en verde contra el build real (Performance 98 · Accesibilidad 100 · Best Practices 92 · SEO 100 · LCP 2,2 s), con prueba negativa de la lógica de umbral (`test-lighthouse-gate.ts`, R-26) — arrancar Chrome real en cada push solo para probar que el freno puede fallar habría sido caro sin añadir confianza, porque lo que puede tener un defecto es la comparación de umbral, no Lighthouse. Rutas medidas hoy: solo Home, la única página pública que existe en M0-A; DU-07 añade una página de servicio y un artículo cuando existan, tal como exige RNF-01. |
| D-51 | 2026-09-09 | **La aplicación usa temporalmente `MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD` como credencial S3**, en vez de una clave con alcance mínimo. Registrado como riesgo **R-41**, con la misma condición de S-01/R-08: revisar y rotar a una clave acotada antes del go-live. | Instalar y usar la CLI `mc` para crear una clave con `mc admin accesskey create`, acotada a los buckets `downloads`/`deliverables` | Elección de Ricardo. El Console gratuito de MinIO **ya no ofrece gestión de claves de acceso desde la interfaz web** (cambio de producto de MinIO en 2026, no un error suyo ni del agente): la única vía es la CLI `mc`, que exige acceso por terminal al VPS o al contenedor de `minio`, del que no dispone ahora mismo. Usar la credencial de administrador desbloquea FU-09 sin ese paso adicional. **Coste aceptado**: un fallo en la capa de la aplicación podría dar acceso de administrador completo a MinIO, no solo a los dos buckets — por eso queda como riesgo abierto (R-41), no como decisión cerrada sin más. |
| D-52 | 2026-09-10 | **FU-06 usa Better Auth sin los plugins `organization`, `admin` ni `apiKey`** — desviación técnica del enunciado de la unidad, decidida por el agente dentro del margen de una FU (no una elección de producto ni de stack: Better Auth en sí ya estaba decidido). | Los tres plugins tal como los nombra la unidad | El esquema real de FU-04 (`membership`, `api_key`) es más simple que lo que esos plugins asumen, y en un punto los contradice directamente: `organization` espera roles `admin`/`member` y cambio de organización activa, pero el CHECK real de `membership.org_role` guarda los cuatro roles de `user.role` y `data_model.md` es explícito en que B.3 se evalúa **siempre** sobre `user.role`, nunca sobre el rol del plugin — usarlo habría exigido reescribir esa CHECK o ignorar la mitad de lo que ofrece. `admin` añade columnas (`banned`, `ban_reason`, `ban_expires`, `impersonated_by`) que no existen y que ningún criterio de esta unidad exige. `apiKey` ata cada clave a un `user_id` propietario; el esquema real de `api_key` no tiene esa columna **a propósito** — RF-146 es explícito en que el rol `agent` "no es un `user`". La garantía real de R-19 —un módulo propio, único que sabe de sesión, rol, `organization_id` y verificación de clave— se cumple igual: `lib/auth/` completo. |
| D-53 | 2026-09-10 | **Dos políticas de fila adicionales** (migraciones 0005 y 0006) permiten `app_actor_role() = 'system'` sobre `api_key` (completa) y `membership` (solo lectura) — se combinan con OR sobre la política uniforme de 0001, no la sustituyen. | Ampliar la política uniforme de 0001 para las ocho tablas a la vez | Encontrado escribiendo y **probando** FU-06 (`scripts/db/test-auth.ts`), no en el diseño: verificar una clave de API entrante y resolver la empresa de un usuario ya autenticado son, las dos, lecturas **anteriores** a tener el contexto que la política de 0001 exige para leer — sin esta política, ninguna clave habría verificado nunca, ni siquiera las de SLG, y ningún `client_*` habría resuelto nunca su propia empresa. Acotado a solo estas dos tablas y, en `membership`, a solo `SELECT`: `withSystemScope` ya exigía un motivo escrito por uso (`lib/db/scope.ts`, FU-04), así que la puerta no es nueva — lo nuevo es que dos casos de arranque de identidad ahora pueden usarla donde antes las ocho tablas trataban `system` igual que cualquier rol sin permiso. |
| D-54 | 2026-09-10 | **FU-08 se construye con valores PROVISIONALES de P-3 y P-4**, puestos por el agente en modo de ejecución autónoma (instrucción explícita de Ricardo: no bloquearse, dejar para después lo que pueda resolverse después) — no son la resolución final de esas sub-decisiones. **P-3**: se adopta la opción que los propios `design_docs` ya recomendaban (`From` en el subdominio de envío, `Reply-To` a `support@softlandingglobal.com`) — no es una elección nueva del agente, es la que `architecture` §10.3 y `api_contracts` §11.3 ya escribían como recomendada. **P-4**: se usa el ejemplo ilustrativo de D-24 (`mail.softlandingglobal.com`, con sufijo `.test` para las pruebas locales) a falta de mejor dato — este sí es un valor inventado, sin más base que "hacía falta un nombre para poder construir". **Ningún valor entra en el repositorio**: viven solo en `.env` local (ignorado por git), nunca en `.env.example` (que solo lleva `MAIL_SMTP_HOST`, `MAIL_FROM_ADDRESS`, etc. como nombres). | Detener la construcción de FU-08 hasta que Ricardo confirme P-3/P-4 (lo que exigían literalmente `user_units.md` §FU-08 y `api_contracts` §11.3: "se registra en decision_log antes de construirla") | `user_units.md` y `docs/decision_log.md` (antes de esta entrada) fijaban P-3/P-4 como condición de ENTRADA a FU-08, no solo de cierre. `docs/project_memory.md` (escrito al cerrar FU-06) ya suavizaba esto a "no bloquea empezar, sí bloquea el primer envío real y el cierre" — pero seguía sin resolver P-3/P-4 de verdad. Ricardo pidió avanzar sin bloquearse y dejar un recordatorio explícito para lo diferible; esto ES lo diferible: P-3/P-4 no cambian una sola línea de código del adaptador (solo el valor de `MAIL_FROM_ADDRESS`), así que construir y probar el mecanismo con un valor provisional tiene coste de reversión CERO. **Lo que este valor provisional NO desbloquea**: el criterio 3 de FU-08 (entrega real a tres buzones de proveedor) y el criterio 6 (seguimiento desactivado, verificado en el panel de Resend) siguen exigiendo el dominio verificado de **F.2-4**, que no depende de P-3/P-4 y sigue `[PENDIENTE]`. FU-08 queda `in_progress`, no `done`, hasta que Ricardo confirme P-3/P-4 (aunque sea ratificando este mismo valor) y F.2-4 se resuelva. Ver `docs/work_log.md` (entrada FU-08). |
| D-55 | 2026-09-10 | **`invitation` completada contra `data_model` §5.7** (columnas `status`, `inviter_id`, `sent_at`, `accepted_by_user_id`, `revoked_at`, `revoked_by_user_id`; restricciones y los índices parciales que faltaban) y añadida `invitation_insert_policy` **RESTRICTIVE**: un `client_admin` no puede insertar una invitación con `role` de SLG ni hacia una organización de tipo `slg`, ni saltándose la capa de aplicación. `membership` gana una política adicional de `INSERT` para `system` (aceptar una invitación crea la primera membership de una cuenta, antes de que exista contexto de empresa). | Dejar `invitation` como estaba y resolver revocación/reenvío/quién-invitó sin esas columnas | Mismo patrón que D-53 (FU-06) y el hallazgo de `email_delivery` en FU-08: hueco real de FU-04, encontrado escribiendo y **probando** FU-07 (`scripts/db/test-invitations.ts`), no en el diseño. Sin `status`/`revoked_at` no hay forma de expresar "revocada" ni "reenviable"; sin la política RESTRICTIVE, la única barrera contra que un `client_admin` invite un `slg_admin` sería la capa de aplicación (`exigir`), que `data_model` §5.7 exige reforzar en la base ("se hace cumplir en dos capas"). La prueba EXHAUSTIVA de esta política es explícitamente de FU-13; FU-07 prueba el caso positivo y el negativo directo, no la batería completa. |
| D-56 | 2026-09-10 | **`socialProviders.google`/`.microsoft` llevan `disableImplicitSignUp: true`** (`lib/auth/config.ts`). Se reactiva SOLO para la sesión de aceptación de una invitación, pasando `requestSignUp: true` en esa llamada concreta (`lib/auth/client.ts`, botones de `/invitacion/[token]`). | Dejar los proveedores sociales sin este flag, confiando solo en `emailAndPassword.disableSignUp` | Hallazgo propio de FU-07, verificado leyendo el código fuente instalado de Better Auth (`node_modules/better-auth/dist/context/create-context.mjs` y `.../api/routes/callback.mjs`), no solo su documentación: `emailAndPassword.disableSignUp` (F.1, "sin alta pública") es un flag DISTINTO que el manejador de OAuth nunca lee. Sin `disableImplicitSignUp`, cualquiera con una cuenta de Google o Microsoft habría podido crear sesión la primera vez que iniciara sesión — Better Auth se la crea sola — saltándose por completo "acceso de clientes SOLO por invitación" (`data_model` §10-10) para los dos métodos sociales. Nunca se llegó a explotar: F.2-2/F.2-3 siguen sin credenciales reales, así que los proveedores nunca se registraron en producción — pero el hueco existía en el código desde FU-06 y habría quedado activo el día que Ricardo cargara esas credenciales, sin que nada lo señalara. |
| D-57 | 2026-09-10 | **CSP con nonce por petición (`proxy.ts`), no la CSP estática que vivía en `next.config.ts`.** Consecuencia obligada, documentada por Next.js: **toda la aplicación pasa a renderizado dinámico** (`app/layout.tsx`, `dynamic = "force-dynamic"`) — un nonce distinto en cada petición es incompatible con la generación estática y con ISR. | Mantener `script-src 'self'` sin nonce (lo que había) · añadir `'unsafe-inline'` a `script-src` para no tocar el renderizado | Hallazgo propio de FU-07, y grave: la CSP estática (`script-src 'self'`, sin nonce ni `unsafe-inline`) bloqueaba la hidratación de React en **todas** las páginas del sitio, la portada incluida — verificado en el navegador real, con y sin el cambio, contra el build de producción (`.next/standalone`), no solo en teoría. Nadie lo había notado: ninguna unidad hasta FU-07 enviaba un Client Component con interactividad real (botón, formulario) que lo hiciera visible; una página puramente estática "se ve bien" incluso con la hidratación rota. `'unsafe-inline'` en `script-src` habría sido la salida fácil, pero es exactamente lo que una CSP estricta existe para prohibir (ejecución de script inyectado). **Verificado que el coste de rendimiento es nulo o negativo**: el gate D1 (Lighthouse, D-50) se midió antes y después — Performance 98→100, Best Practices 92→100 (las violaciones de CSP en consola descontaban puntos que nadie había atado a esta causa), LCP 2,3 s→1,2 s. `style-src` se queda en `'unsafe-inline'` sin nonce a propósito: React aplica atributos `style="..."` en línea, y un nonce no cubre atributos de estilo por especificación CSP3 (solo elementos `<script>`/`<style>`) — verificado también en el navegador, con errores de consola distintos y explícitos sobre esto. |

| D-58 | 2026-09-10 | **`MobileSheet.tsx` (FU-10, componente #2 de C.5) deja de pasar `y` como `useMotionValue` externo por `style`**; `drag`/`dragConstraints`/`dragElastic`/`animate`/`exit` gestionan la posición internamente, sin valor externo. Se añade `key` explícita a los dos `motion.div` hijos de `AnimatePresence` (antes sin ella). | Mantener el valor externo para poder leerlo fuera del gesto | Hallazgo propio de FU-10, verificado en el navegador real: con un `useMotionValue` externo enlazado por `style={{ y }}`, Motion **no** deja que `animate`/`exit` controlen ese mismo valor — queda "reservado" para quien lo pasó por `style`. El cierre del sheet (clic en el scrim, tecla Escape, botón de cerrar) cambiaba el estado de React correctamente (`aria-expanded` pasaba a `false`) pero la hoja se quedaba visualmente abierta, congelada en `transform: none` — nunca llegaba a moverse hacia la posición de salida. `alSoltar` (el handler de `onDragEnd`) no necesitaba leer `y` directamente —solo usa `info.offset.y`/`info.velocity.y` del gesto—, así que quitar el valor externo no pierde nada. **Verificación parcial, no completa**: tras el arreglo, el valor de `y` sí anima correctamente hacia la posición de salida (confirmado con la animación desactivada, en tween lineal) — pero el desmontaje final del nodo del DOM no se pudo confirmar de forma concluyente en esta sesión porque el panel del navegador se mantuvo con `document.visibilityState === "hidden"` durante toda la verificación (Ricardo seguía la sesión desde el teléfono, sin el panel abierto en pantalla), y Chromium suspende las animaciones dirigidas por `requestAnimationFrame` — el mecanismo que usa Motion — en pestañas en ese estado. Es una limitación del entorno de verificación, no evidencia de que el defecto siga presente: la causa raíz identificada (el valor externo bloqueando `animate`/`exit`) está corregida y es coherente con el comportamiento documentado de Motion. **Pendiente**: repetir la comprobación de cierre con el panel visible en pantalla antes de dar por cerrado el criterio 1 (gesto completo) de este componente. |

| D-59 | 2026-09-10 | **Reordenación de FU-10 declarada**: los nueve componentes de C.5 se construyen en el orden que ya fija el propio texto de la unidad — formulario de descarga primero (RF-134), navegación segundo, y el resto en el orden literal de C.5 (hero, tarjeta de rama/servicio, «qué incluye», tarjeta de artículo, pie, app shell, visor de entregables). | Construir en el orden literal de C.5 sin adelantar el formulario | El propio `implementation/user_units.md` (FU-10) instruye registrar esta reordenación en `decision_log` antes de construir: "Esta reordenación se declara y se registra en `decision_log`." No es una decisión nueva del agente — el texto de la unidad ya resuelve el orden, esta entrada solo cumple la instrucción literal de dejarlo escrito aquí. |

| D-60 | 2026-09-11 | **P-3 y P-4 confirmadas por Ricardo, con valor final DISTINTO del provisional de D-54.** P-3: `From` en el subdominio de envío, `Reply-To` a `support@softlandingglobal.com` (la opción recomendada, ratificada). P-4: el subdominio verificado en Resend es **`mailweb.softlandingglobal.com`** — no `mail.softlandingglobal.com`, el ejemplo inventado que D-54 había usado para poder construir. Dominio con estado **Verified** en el panel de Resend; recepción de correo ("enable receiving") queda apagada a propósito — este subdominio es solo de envío transaccional, nunca recibe. | Mantener el valor de ejemplo de D-54 sin verificar contra lo que Ricardo configuró realmente en Resend | Ricardo gestionó el alta del dominio en Resend directamente y reportó el nombre real y el estado `Verified`. Sustituye el valor provisional de D-54 en todo lugar donde aparezca fuera del código (el código nunca tuvo el valor literal — D-54 ya dejaba claro que vivía solo en `.env`, nunca en el repositorio) — `MAIL_FROM_ADDRESS` en `.env`/Easypanel debe usar `@mailweb.softlandingglobal.com`, no `@mail.softlandingglobal.com`. Cierra F.2-4 en conjunto con la verificación del dominio: FU-08 puede ahora completar su criterio 3 (entrega real) y criterio 6 (seguimiento desactivado, verificado en el panel de Resend) en cuanto se confirme el envío real de una prueba. |

| D-61 | 2026-09-11 | **Primer borrador de FU-01 (copy maestro) redactado por el agente**, con autorización explícita de Ricardo ("redacta todo lo que esté pendiente... sigue hasta terminar"). Cubre: Home, los 4 overviews de rama, los 11 registros `service` (secciones 1–3; las 4–6 ya eran texto final y se replicaron sin cambio) con sus 11 registros `download` completos (10 nuevos), Doctrina (page shell) y Nosotros — más el cierre de ~29 cadenas de `content/ui` que ya tenían un borrador funcional del agente y no necesitaban dato externo. **No cierra la compuerta de FU-01**: sigue pendiente la aprobación explícita de Ricardo (criterio 6) y, en varios puntos, dato que el agente no tiene y no inventó — ver la lista de `[PENDIENTE]` que deja este borrador, más abajo. | Esperar a que Ricardo redactara el copy desde cero, o a que SLG_Overhauling produjera `copy-maestro.md` de forma independiente | Ricardo autorizó explícitamente esta redacción tras pedir el agente instrucciones más específicas sobre qué había que redactar. La fuente usada es real y verificable, no inventada: `~/Dev/SLG_Overhauling/SLG Overhauling.md` (estructura de la oferta, escrita por Ricardo) para los servicios y ramas; `knowledge/doctrine-summary.md`, `knowledge/offer-structure.md` y `knowledge/naming-rules.md` (ya en este repositorio) para estructura, nomenclatura y límites duros. **Disciplina aplicada, no relajada**: donde la fuente no daba un dato verificable —contenido específico de 6 de los 11 servicios, los títulos de los documentos D-02…D-11, la cita de The Phoenix Doctrine, la biografía y mentorías de Nosotros, qué documento se destaca en Home— el borrador deja `[PENDIENTE: …]` explícito en vez de inventar, exactamente como exige el criterio 3 de FU-01 y la regla dura de `doctrine-summary.md` ("nada de lo que falta se rellena por inferencia"). Verificado contra los propios gates del proyecto, no solo por inspección: `npm run check:content` (frontmatter, paridad ES/EN, nomenclatura literal, marcadores `[PENDIENTE]`) y `npm run verify` completo, ambos en verde. **Hallazgo propio en el camino**: `lib/content/schema.ts` marcaba `parent: null` como "campo obligatorio vacío" pese a que su propio validador (`isPair`) declara `null` válido — el código eximía el `null` solo para el campo literalmente llamado `pair`, no para cualquier campo que use `isPair` como validador. Sin el arreglo, el registro real de `SLG_Holdings` (branch sin overview padre, caso legítimo que la propia función ya preveía) no habría podido existir. Corregido identificando la excepción por la función de validación (`check !== isPair`), no por el nombre del campo — confirmado con la batería de pruebas negativas de `npm run verify`, que sigue en verde. |

| D-62 | 2026-09-11 | **`--blue-deep` se oscurece de `#24394D` a `#182430`** (mismo matiz ~209-210°, luminosidad 22%→14%). Contraste sobre `--paper`: 11,9:1 → 15,7:1. | Mantener `#24394D` (D-47) | Elección de Ricardo, viendo el hero y los títulos renderizados con el panel del navegador ya visible: los encontró "muy tenues" para una agencia que debe proyectar "firmeza y elegancia a la vez". No es un cambio de matiz (seguiría siendo el mismo azul sobrio de D-47, no uno nuevo) sino de peso visual. Verificado que no cuesta nada del gate D1: Lighthouse sigue en verde (Performance 98, Accesibilidad 100, Best Practices 100, SEO 100, LCP 2,3s) — y el contraste, si acaso, mejora. |

| D-63 | 2026-09-11 | **FU-11 construida**: `lib/anti-abuse/` (dominios de correo gratuito, límite de peticiones, honeypot). Cierra los dos pendientes que `data_model.md` (#4, #5) y `api_contracts.md` §11.8 (#6, #7) dejaban abiertos para esta unidad: la lista de dominios vive en `blocked_email_domain` (Postgres, editable con un `INSERT`, sin desplegar — RF-32); el límite de peticiones vive en `rate_limit_event` (una fila por intento, ventana deslizante, RF-34). Ambas tablas con RLS: solo `system`, sin `organization_id` (no son datos de ninguna empresa). Migraciones `0009_fu11_anti_abuso.sql` (drizzle-kit) y `0010_anti_abuso_rls.sql` (a mano). | Almacén en memoria para el límite de peticiones (RF-34 dejaba la puerta abierta) | Ricardo autorizó avanzar sin consultarle unidades no bloqueadas mientras estaba fuera; FU-11 no depende de FU-01/FU-10 ni de ningún dato externo — solo de FU-04/FU-05, ya `done`. Un almacén en memoria se descartó porque no sobrevive un reinicio ni se comparte entre instancias si el sitio llega a escalar horizontalmente; Postgres ya es la única pieza de infraestructura con estado de todo el proyecto (D-16: "propio, sin terceros"), así que una tabla más no añade una dependencia nueva. **Hallazgo propio, corregido antes de terminar**: la primera versión de `verificarLimiteDePeticiones` contaba y luego insertaba en dos sentencias separadas — dos peticiones simultáneas para la MISMA clave podían leer el mismo conteo antes de que ninguna insertara la suya, colando una petición de más justo en el umbral. Corregido con `pg_advisory_xact_lock` sobre un hash de la clave (serializa solo a quienes comparten IP/correo, no a peticiones de terceros); **verificado, no solo argumentado**: una prueba de 10 peticiones concurrentes contra un umbral de 5 permite exactamente 5 (`scripts/db/test-anti-abuse.ts`, 16 comprobaciones, todas contra Postgres real). **Queda fuera de esta unidad, y se declara por qué**: el criterio 5 (cero scripts de terceros en 27 rutas) y el criterio 6 (validar contra esquema antes de usarse) no se pueden cerrar todavía — las 27 rutas no existen (DU-02/03 en adelante) y no hay una Server Action real que reciba esta entrada (DU-08). Se cierran cuando ese llamador real exista, no aquí. **Hallazgo aparte, documentado para quien escriba la próxima migración**: el journal interno de `drizzle-kit` (`drizzle/meta/_journal.json`) solo registraba `0000` y `0004` — las siete migraciones escritas a mano (0001, 0002, 0003, 0005…0008) nunca se registraron ahí. `drizzle-kit generate` intentó numerar esta migración como `0002` (colisión real con `0002_rol_de_aplicacion.sql`) y su SQL generado repetía columnas/restricciones que 0007/0008 ya habían añadido a mano. Se renombró a `0009`, se corrigió el `tag` del journal, y se depuró el SQL a mano para dejar solo lo nuevo — pero el journal sigue sin conocer 0001-0003/0005-0008: la próxima vez que alguien corra `drizzle-kit generate`, hay que repetir esta misma comprobación (diff manual contra lo ya aplicado) antes de aceptar el archivo que genere. No se intentó reconciliar el journal completo en esta sesión: es un cambio de mayor riesgo que no pedía nadie y que merece su propia revisión cuidadosa. |

| D-64 | 2026-09-11 | **FU-01 cerrada**: Ricardo aprueba explícitamente el borrador de copy maestro ("Apruebo todos los copy"), registrado con fecha en `work_log.md` (criterio 6). DU-02/DU-03 quedan desbloqueadas. | — | Aprobación directa de Ricardo. Detalle completo, incluida la lista de `[PENDIENTE]` que la aprobación NO resuelve por sí misma (Doctrina, biografía de Nosotros, documento destacado en Home, 6 servicios, D-02…D-11), en `docs/work_log.md`, entrada "FU-01 — Aprobación explícita de Ricardo". |
| D-65 | 2026-09-11 | **Patrón por defecto para tokens de R2 (FU-14), confirmado contra la pantalla real de Cloudflare**: permiso `Object Read & Write` (no existe un nivel "escribe pero no borra" — es lo más ajustado que ofrece R2), acotado a **un bucket específico** (nunca "apply to all buckets"), TTL `Forever`, nombre descriptivo del proceso que lo usa (`slg-backup-write`, `slg-backup-purge`). Cada token se guarda por separado; el proceso de copia nunca tiene acceso al de purga. | Un token único con un solo permiso para ambos procesos | Corrección en el sitio a la instrucción original (que asumía, sin haberlo comprobado, un nivel de permiso "sin borrado" que Cloudflare no ofrece). Verificado navegando la pantalla real de creación de token en la cuenta de Ricardo: los únicos niveles con escritura son `Admin Read & Write` y `Object Read & Write`, y ambos incluyen borrado. La mitigación de R-37 (sin Object Lock nativo en R2) pasa a ser de procedimiento —credenciales separadas, cada una solo en manos del proceso que le corresponde— no una restricción que Cloudflare aplique por sí sola. Bucket `slg-backups` ya creado por Ricardo; **los dos tokens (`slg-backup-write`, `slg-backup-purge`) creados y confirmados** — el agente configuró cada pantalla, Ricardo dio el clic final y guardó cada secreto por separado en su gestor de contraseñas (el agente nunca vio ninguno de los dos, a propósito). |
| D-66 | 2026-09-11 | **Resuelto el `[PENDIENTE: mecanismo exacto de programación en la plataforma]` de `design_docs/architecture.md` §9.1, y generada la clave de cifrado de FU-14.** Verificado contra la documentación oficial de Easypanel (`docs/guides/cron-job`, `docs/backups`, `docs/backups/database`, `docs/backups/volumes`, `docs/storage-providers`): Easypanel **no** ofrece un programador nativo para scripts arbitrarios — su guía de "Cron Job" solo cubre (a) hornear un crontab en el `Dockerfile` de un servicio que corre `crond -f`, o (b) un servicio de cron de terceros (`cron-job.org`) disparando un endpoint HTTP. Sí tiene un backup nativo programable (`Database Backups`/`Volume Backups`: preset o expresión cron, retención, S3-compatible incluido Cloudflare R2 explícitamente), pero **no sirve para FU-14**: sube el volcado solo comprimido (`pg_dump` + gzip), sin cifrar antes de subir, y su credencial de destino necesita permiso de listar/leer/borrar para aplicar la retención — viola la mitigación 1 de R-37 (§8.3/§9.2: credencial de solo escritura) y el criterio 3 de FU-14 (cifrar en el VPS antes de subir); además requiere una licencia de Easypanel que incluya backups programados, sin confirmar en el plan actual. **Mecanismo elegido**: un servicio **App aparte** en Easypanel (nunca dentro de `slg-web`), con su propio `Dockerfile` que hornea el crontab y ejecuta el script de copia (volcado → cifra con la clave de este mismo D-66 → sube por el puerto de §8.3 con el token `slg-backup-write`, de solo escritura). **Clave de cifrado generada** con `openssl rand -hex 32` (256 bits) en esta sesión y entregada a Ricardo fuera del repositorio, para su gestor de contraseñas y, al construir FU-14, como variable de entorno **solo del servicio de copia** — nunca en `slgweb-staging`/`slg-web` ni en el repositorio. | (1) Backups nativos de Easypanel a R2 — descartada por no cifrar antes de subir y exigir credencial con borrado; (2) Cron de terceros (`cron-job.org`) contra un endpoint HTTP — descartada por introducir un tercero disparable desde fuera en un proceso con credenciales de escritura sobre backups, mismo motivo por el que §7 (tabla de opciones del barrendero de colas) ya rechazó un "disparador externo por HTTP" para otro proceso en segundo plano (Frontera h, R-09) | Es la única opción de las tres que no rompe ninguna de las cuatro mitigaciones de R-37 ya fijadas (§9.2) ni el criterio 3 de FU-14, y no depende del nivel de licencia de Easypanel. Con esto, y con los dos tokens de R2 de D-65, **FU-14 queda listo para construirse**; falta solo, ya al construir, confirmar en el propio panel el timezone del servidor (el cron sigue la hora del servidor, no UTC por defecto). |
| D-67 | 2026-09-11 | **Bloque 5 de Home (últimos artículos) NO se renderiza cuando está vacío; el bloque 6 (descarga destacada) SÍ conserva estado vacío redactado.** Resuelve una contradicción real entre `ui_wireframes` §2.1 —«la sección no se renderiza; una portada con estados vacíos visibles no es una portada»— y el criterio 2 de DU-03, que pide redacción propia para *ambos* bloques. `design_summary` no la cataloga entre sus conflictos. | Aplicar el criterio 2 a los dos bloques (dos huecos con «próximamente» en la portada); u ocultar también el bloque 6 | Para el bloque 5 gana el wireframe: es la instrucción **específica para esta situación exacta**, está razonada, y una portada de agencia con una franja vacía de artículos se lee como sitio a medio hacer. Para el bloque 6 gana el criterio: es el **único CTA de la portada** (RF-07) y ocultarlo dejaría la página sin camino de conversión — un estado vacío escrito es peor que un documento, pero mucho mejor que ninguna salida. Reversible en un `if`: si Ricardo prefiere ver el bloque 5 con texto propio, es un cambio de una línea en `components/home/Home.tsx`. |
| D-68 | 2026-09-11 | **Los enlaces del pie pasan de `--blue-primary` a `--blue-deep`**, y se añade la regla `.seccion-oscura a` para enlaces sobre fondos oscuros. | Dejarlo como estaba (el gate D1 pasaba igual, 96 ≥ 90) | No es preferencia estética: son incumplimientos medidos de WCAG AA que el propio `knowledge/brand-tokens.md` ya prohibía por escrito. El pie daba **4,37:1** con enlaces de 14 px sobre `--paper-2` (el token documenta «solo en tamaño grande ≥ 24 px») y afectaba a **todas** las páginas del sitio desde FU-10; la franja oscura daba **3,32:1**. Detalle de la causa de fondo —la regla `a` de `globals.css` está fuera de toda `@layer` y por eso gana a las utilidades de Tailwind v4, de modo que colorear un enlace con `className` no surte efecto— en `docs/work_log.md`, DU-03. |
| D-69 | 2026-09-11 | **Tercera credencial de R2, de solo lectura, para restaurar** (`slg-backup-restore`, permiso `Object Read only`, acotado a `slg-backups`, TTL `Forever`). El código lee `R2_ACCESS_KEY_ID_RESTORE`/`R2_SECRET_ACCESS_KEY_RESTORE` **sin recurso ni valor por defecto** a los otros dos tokens. **El token todavía no existe: lo tiene que crear Ricardo.** | Reutilizar el token de escritura, que en R2 sí puede leer (D-65: no existe un nivel «escribe pero no borra») | `architecture` §8.3 exige que restaurar y purgar sean procesos distintos con credenciales distintas, y §9.3 paso 1 manda listar «con la credencial de LECTURA (distinta de la de copia)». D-65 creó dos tokens y ninguno es ese. Reutilizar el de escritura haría que el token que corre a diario en un servicio desatendido fuese también el que abre las copias: una filtración valdría para las dos cosas. Sin este token, `restore-backup.ts` no arranca — a propósito. |
| D-70 | 2026-09-11 | **Dos servicios App en Easypanel (`slg-backup` y `slg-backup-purga`), no uno con dos entradas de cron.** Concreta D-66, no lo contradice: donde decía «un servicio App aparte», son dos. | Un solo contenedor con dos líneas de crontab | Un contenedor único llevaría en su entorno **las dos credenciales**, y la mitigación 2 de R-37 pide que la purga la haga un proceso distinto con otras credenciales: juntas, quien comprometa el VPS puede borrar los backups además de los datos — el escenario exacto que R-37 describe. Además cada imagen instala solo lo suyo: la de purga no lleva `postgresql16-client`, ni `tar`, **ni la clave de cifrado**, así que no puede abrir un backup ni estando comprometida. Verificado en la imagen construida: `command -v pg_dump` no encuentra nada. |
| D-71 | 2026-09-11 | **El paquete de volúmenes guarda el PAPEL del cubo (`downloads`/`deliverables`), nunca su nombre.** Cada entorno traduce el papel a los cubos que diga su propia configuración. Consecuencia: el orden de `FILES_BUCKET_DOWNLOADS` y `FILES_BUCKET_DELIVERABLES` es significativo. | Guardar `<nombre-del-cubo>/<clave>`, que es como estaba escrito en la primera versión | **Hallazgo propio con consecuencia de seguridad real**: un paquete hecho en producción lleva dentro los nombres de producción, así que restaurarlo en staging habría subido los objetos **de vuelta a producción** — el peor fallo posible en un procedimiento de emergencia. Con el papel en vez del nombre, el nombre de producción no está escrito en ninguna parte del paquete y no hay forma de acabar allí ni por descuido. La prueba usa cuatro cubos con nombres distintos para que esto no pueda pasar desapercibido. |
| D-72 | 2026-09-11 | **La retención cuenta EJECUCIONES, no objetos, y borra por conteo, nunca por antigüedad.** Una ejecución deposita tres objetos (volcado, archivos, manifiesto) y los tres se conservan o se van juntos. La purga nunca borra una clave que no reconoce: la enumera para que alguien decida a mano. | Contar objetos; caducar por fecha | Un volcado sin sus archivos, o sin el manifiesto con el que comparar recuentos (§9.3 paso 6), es media copia: contar objetos habría dejado «14 diarias» en cuatro días y medio de historia. Y borrar por antigüedad significa que si el proceso de copia lleva un mes caído, la purga **vacía el destino** por caducidad — un borrado que depende del reloj es exactamente el mecanismo que R-37 teme. Por conteo, las N últimas siguen siendo las N últimas. |

## Acciones de seguridad previas a la ejecución

| # | Acción | Dónde se sigue |
|---|--------|----------------|
| S-01 | Existe una **acción de seguridad previa a la ejecución**, ya identificada y asignada a Ricardo. **Nada relacionado con ella se escribió en ningún archivo del repositorio** — verificado. | **Diferida por decisión de Ricardo (2026-09-08)**: se ejecuta al final del proyecto, no ahora. **Condición innegociable que hace aceptable el aplazamiento**: la credencial afectada es de **construcción**, no de producción; las credenciales de producción se crean nuevas cuando se necesiten y **nunca** se reutiliza la afectada en ningún entorno desplegado. La rotación es requisito de go-live: sin ella, el DoD no se da por cumplido. **Fuera de este repositorio.** El detalle, el estado y el seguimiento operativo se llevan por el canal privado: este repositorio es **público** (§10-6) y un registro público de una acción de seguridad es, por sí mismo, información útil para un atacante. |

## Pendientes de decisión

Las elecciones de producto de **correo transaccional** (D-22) y **destino de backups** (D-21) están
cerradas, **P-5 incluida** (D-43). Quedan solo dos sub-decisiones de configuración, que se fijan en M0:

| # | Pendiente | Tipo | Estado |
|---|-----------|------|--------|
| ~~P-5~~ | **Monitorización externa de disponibilidad, fuera del VPS.** La abrían `risks.md` R-29 y RF-130 (gate D11). | Elección de **categoría** | ✅ **Cerrada por D-43**: servicio de uptime dedicado con tramo gratuito, externo al VPS. Queda elegir el producto concreto con 2–3 candidatos (paso 5), lo que **no bloquea el arranque**: afecta a FU-05 y DU-25, ambas fuera de M0-A. n8n queda como monitor **secundario**. |
| ~~P-3~~ | Dirección remitente visible, tras D-24: `From` en el subdominio con `Reply-To` a `support@` (recomendado), o `From` en la raíz apoyándose solo en alineación DKIM | Sub-decisión de configuración | ✅ **Cerrada por D-60** (2026-09-11): confirmada la opción recomendada — `From` en el subdominio, `Reply-To` a `support@softlandingglobal.com`. |
| ~~P-4~~ | Nombre exacto del subdominio de envío | Sub-decisión de configuración | ✅ **Cerrada por D-60** (2026-09-11): `mailweb.softlandingglobal.com` — **distinto** del ejemplo provisional de D-54 (`mail.softlandingglobal.com`); Ricardo verificó este dominio en Resend, no el de ejemplo. |

**Ninguna bloquea la aprobación del plan.** P-3 y P-4 quedan cerradas por D-60.
El producto de monitorización se elige antes de FU-05, con el mismo procedimiento que P-1 y P-2:
2–3 candidatos con contrapartidas, y elige Ricardo.

Elecciones de producto resueltas con Ricardo el 2026-09-08 (AGENTS.md Regla 7: elige quien vive las consecuencias):

| # | Categoría decidida | Falta elegir | Ref. |
|---|--------------------|--------------|------|
| ~~P-1~~ | ~~Servicio transaccional dedicado (D-15)~~ | ✅ **Resuelta: Resend (D-22)** | — |
| ~~P-2~~ | ~~Object storage externo (D-20)~~ | ✅ **Resuelta: Cloudflare R2 (D-21)** | — |
